# Sprint50 JRN-007 entry/schema-gate historical compatibility anchor.
# Author by Lab | zefry
# Sprint49 JRN-006 active-shift schema/source-envelope compatibility preservation anchor.
# Sprint49 JRN-006 active-shift precondition entry-gate compatibility preservation anchor.
# Sprint48 JRN-005 Sprint35 JRN-006 compatibility preservation anchor.
# Sprint48 JRN-005 Sprint46 upper-horizon preservation anchor.
# Sprint48 JRN-005 Sprint40-Sprint42 nine-path preservation anchor.
# Sprint48 JRN-005 Sprint36 compatibility preservation anchor.
# Sprint48 JRN-005 Sprint32 one-path upper-horizon preservation anchor.
# Sprint48 JRN-005 M7.4 two-path upper-horizon preservation anchor.
# Sprint48 JRN-005 exact three-path Sprint35/Sprint42 compatibility preservation anchor.
# Sprint48 JRN-005 three-path migration-17 compatibility preservation anchor.
# Sprint48 JRN-005 schema/source-envelope gate compatibility preservation anchor.
# Sprint48 JRN-005 entry-gate compatibility preservation anchor.
# Sprint47 JRN-004 frozen source upper compatibility preservation anchor.
# Sprint47 JRN-004 canonical migration #16 horizon preservation anchor.
# Sprint47 JRN-004 schema-gate exact five-workflow preservation anchor.
# Sprint47 JRN-004 entry-gate compatibility preservation anchor.
# Sprint46 post-source docs migration-horizon preservation anchor.
# Sprint46 JRN-006 exact source-surface compatibility preservation anchor.
# Sprint46 JRN-006 upper-horizon exact preservation anchor.
# Sprint46 JRN-006 ab09 compatibility preservation anchor.
# Sprint46 JRN-006 sale completion schema/source-envelope gate compatibility preservation anchor.
# Sprint46 Business MVP POS core transaction and outlet operations entry-gate compatibility preservation anchor.
# Sprint45 Sprint36 exact source-compatibility predecessor anchor.
# Sprint45 pending-MFA eligibility schema/source-envelope gate compatibility preservation anchor.
# Sprint45 pending-MFA identity-eligibility entry-gate compatibility preservation anchor.
# Sprint44 Sprint33/Sprint34 exact compatibility predecessor anchor.
# Sprint44 source four-path compatibility predecessor anchor.
# Sprint43 fd5 post-Sprint42 legacy compatibility preservation anchor.
# Sprint41 Sprint38 source-successor route-guard correction anchor.
# Sprint41 Sprint38 legacy-horizon executable-isolation correction anchor.
# Sprint41 Sprint38 legacy-horizon migration-state correction anchor.
# Sprint41 Sprint32-Sprint34 upper-horizon compatibility preservation anchor.
# Sprint41 schema-gate Sprint38 canonical-state correction anchor.
# Post-Sprint39 M7.5 seven-workflow preservation predecessor anchor.
# Sprint44 schema/source gate five-workflow compatibility predecessor anchor.
name: Sprint39 First-Party Session Organizational Access Revalidation Regression

# Attribution: Lab | zefry
# Sprint40 Sprint38/Sprint39 historical fixture isolation anchor.
# Sprint40 Sprint37 same-step successor-state correction anchor.
# Sprint40 Sprint38 same-step successor-state correction anchor.
# Sprint40 19-workflow preservation-horizon bridge anchor.

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/sprint35-privileged-totp-recovery-regression.yml'
      - '.github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml'
      - '.github/workflows/sprint37-first-party-all-session-termination-regression.yml'
      - '.github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml'
      - '.github/workflows/sprint39-first-party-session-organizational-access-revalidation-regression.yml'
      - 'apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php'
      - 'apps/web/tests/first-party-session-organizational-access-revalidation.php'
      - 'docs/FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_FOUNDATION.md'
      - 'docs/SPRINT_40_FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_ENTRY_GATE.md'
      - 'docs/SPRINT_40_FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_SCHEMA_SOURCE_ENVELOPE_GATE.md'
      - 'docs/SPRINT_41_FIRST_PARTY_IDENTITY_AUTHENTICATION_ELIGIBILITY_ADMINISTRATION_SCHEMA_SOURCE_ENVELOPE_GATE.md'
      - 'docs/SPRINT_42_FIRST_PARTY_IDENTITY_DISABLEMENT_SESSION_TERMINATION_SCHEMA_SOURCE_ENVELOPE_GATE.md'
      - '.github/workflows/sprint41-first-party-identity-authentication-eligibility-administration-regression.yml'
      - '.github/workflows/sprint42-first-party-identity-disablement-session-termination-regression.yml'
      - '.github/workflows/sprint43-first-party-identity-authentication-eligibility-reactivation-regression.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sprint39-first-party-session-organizational-access-revalidation-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  sprint39-first-party-session-organizational-access-revalidation:
    name: sprint39-first-party-session-organizational-access-revalidation
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      APP_NAME: oneQay
      APP_ENV: testing
      APP_KEY: base64:b29vb29vb29vb29vb29vb29vb29vb29vb29vb29vb28=
      APP_DEBUG: 'false'
      APP_URL: http://localhost
      ONEQAY_RUNTIME_CLASS: ci
      ONEQAY_PERSISTENCE_ENABLED: 'false'
      ONEQAY_AUTHENTICATION_SESSION_CONTROL_ENABLED: 'true'
      ONEQAY_PRIVILEGED_TOTP_MFA_ENABLED: 'false'
      SESSION_DRIVER: array
      CACHE_STORE: array

    steps:
      - name: Checkout exact source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Setup exact PHP runtime
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240
        with:
          php-version: '8.3'
          tools: composer:v2
          coverage: none

      - name: Classify exact Sprint37 quote-correction compatibility
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" | sort > /tmp/s39-s37-quote-compat-paths
          count="$(wc -l < /tmp/s39-s37-quote-compat-paths)"
          fingerprint="$(sha256sum /tmp/s39-s37-quote-compat-paths | awk '{print $1}')"
          echo 'ONEQAY_S39_SPRINT37_QUOTE_COMPATIBILITY=false' >> "$GITHUB_ENV"
          if [[ "$count" -eq 1 && "$fingerprint" == '83b1eb2b4585a83825ab8f84457cb2ca945a3dfcaa5abbf82c1341981983ff9a' ]]; then
            echo 'ONEQAY_S39_SPRINT37_QUOTE_COMPATIBILITY=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'Recognized exact Sprint37 quote-correction successor shape.'
          fi

      - name: Classify exact Sprint47 JRN-004 compatibility
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" | sort > /tmp/s39-jrn004-compat-paths
          count="$(wc -l < /tmp/s39-jrn004-compat-paths)"
          fingerprint="$(sha256sum /tmp/s39-jrn004-compat-paths | awk '{print $1}')"
          echo 'ONEQAY_S39_JRN004_COMPATIBILITY=false' >> "$GITHUB_ENV"
          if [[ "$count" -eq 2 && "$fingerprint" == '920918ec3a30e7954bec598ce2d45ce4ce05681f1705308bae87232cd192ee4e' ]] || [[ "$count" -eq 15 && "$fingerprint" == 'c1ac6d5130c9d78ff99ed0accf5d11e3f08debc575c0d5152ef5b24e6f82ce02' ]]; then
            echo 'ONEQAY_S39_JRN004_COMPATIBILITY=true' >> "$GITHUB_ENV"
            for migration in \
              'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php' \
              'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' \
              'apps/web/database/migrations/0000_00_00_000016_create_pos_sale_completion_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000017_create_pos_catalog_preparation_journal.php'
            do
              if [[ -f "$migration" ]]; then
                git update-index --assume-unchanged "$migration"
                rm -f "$migration"
              fi
            done
            echo 'Recognized exact Sprint47 JRN-004 compatibility shape.'
          elif [[ "$count" -eq 7 && "$fingerprint" == '4784ffca1c940d3fa54a2a3988ead07e2de993bde8d3af2bd41014dbdf905be0' ]]; then
            for migration in \
              'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' \
              'apps/web/database/migrations/0000_00_00_000016_create_pos_sale_completion_foundation.php'
            do
              if [[ -f "$migration" ]]; then
                git update-index --assume-unchanged "$migration"
                rm -f "$migration"
              fi
            done
            echo 'Migrations #15-#16 isolated only from exact M7.5 seven-workflow historical preservation.'
          fi

      - name: Enforce exact Sprint39 source envelope
        if: github.event_name == 'pull_request' && env.ONEQAY_S39_SPRINT37_QUOTE_COMPATIBILITY != 'true' && env.ONEQAY_S39_JRN004_COMPATIBILITY != 'true'
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/sprint39-source-paths <<'EOF'
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          .github/workflows/sprint39-first-party-session-organizational-access-revalidation-regression.yml
          apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php
          apps/web/tests/first-party-session-organizational-access-revalidation.php
          docs/FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_FOUNDATION.md
          EOF
          sort -o /tmp/sprint39-source-paths /tmp/sprint39-source-paths
          test "$(sha256sum /tmp/sprint39-source-paths | awk '{print $1}')" = '2cfc92c34f46375b11bf3fe92f9094cefa598d234133847fdd6629be211f12c4'

          cat > /tmp/post-sprint39-reconciliation-preservation-paths <<'EOF'
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          .github/workflows/sprint39-first-party-session-organizational-access-revalidation-regression.yml
          EOF
          sort -o /tmp/post-sprint39-reconciliation-preservation-paths /tmp/post-sprint39-reconciliation-preservation-paths
          test "$(sha256sum /tmp/post-sprint39-reconciliation-preservation-paths | awk '{print $1}')" = 'ab09e044925ae98740a49c46a70ff387e5f5445b245d3a556d85ad6a354305ec'

          cat > /tmp/m7-5-sprint39-seven-workflow-preservation-paths <<'EOF'
          .github/workflows/m7-5-preview-release-artifact.yml
          .github/workflows/sprint33-recovery-bound-password-reset-regression.yml
          .github/workflows/sprint34-authenticated-password-change-regression.yml
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          EOF
          sort -o /tmp/m7-5-sprint39-seven-workflow-preservation-paths /tmp/m7-5-sprint39-seven-workflow-preservation-paths
          test "$(sha256sum /tmp/m7-5-sprint39-seven-workflow-preservation-paths | awk '{print $1}')" = '4784ffca1c940d3fa54a2a3988ead07e2de993bde8d3af2bd41014dbdf905be0'

          cat > /tmp/sprint40-entry-gate-paths <<'EOF'
          docs/SPRINT_40_FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_ENTRY_GATE.md
          EOF
          sort -o /tmp/sprint40-entry-gate-paths /tmp/sprint40-entry-gate-paths
          test "$(sha256sum /tmp/sprint40-entry-gate-paths | awk '{print $1}')" = '7a8c362aa7ec66500ba544911e3fb008a20f5d21ac4d00d6a12ea04749ed2466'

          cat > /tmp/sprint41-entry-gate-paths <<'EOF'
          docs/SPRINT_41_FIRST_PARTY_IDENTITY_AUTHENTICATION_ELIGIBILITY_ADMINISTRATION_ENTRY_GATE.md
          EOF
          sort -o /tmp/sprint41-entry-gate-paths /tmp/sprint41-entry-gate-paths
          test "$(sha256sum /tmp/sprint41-entry-gate-paths | awk '{print $1}')" = '074a842bd6fb56ac06ae4658aeb6596b3674d426c4d58fee594b699820eac5f2'

          cat > /tmp/sprint40-schema-source-envelope-gate-paths <<'EOF'
          docs/SPRINT_40_FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_SCHEMA_SOURCE_ENVELOPE_GATE.md
          EOF
          sort -o /tmp/sprint40-schema-source-envelope-gate-paths /tmp/sprint40-schema-source-envelope-gate-paths
          test "$(sha256sum /tmp/sprint40-schema-source-envelope-gate-paths | awk '{print $1}')" = '9a3a85fb3de7ce007b09586af60e18b0cefd6ec1b6fba1b3c2b64e508354d803'

          cat > /tmp/sprint41-schema-source-envelope-gate-paths <<'EOF'
          docs/SPRINT_41_FIRST_PARTY_IDENTITY_AUTHENTICATION_ELIGIBILITY_ADMINISTRATION_SCHEMA_SOURCE_ENVELOPE_GATE.md
          EOF
          sort -o /tmp/sprint41-schema-source-envelope-gate-paths /tmp/sprint41-schema-source-envelope-gate-paths
          test "$(sha256sum /tmp/sprint41-schema-source-envelope-gate-paths | awk '{print $1}')" = '1a2c18a7ec9b363a9b2ef3bc6ff86366ab58f4812e8968d83642e6ce359f48b2'

          cat > /tmp/sprint40-source-preservation-paths <<'EOF'
          .github/workflows/m7-5-preview-release-artifact.yml
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          .github/workflows/sprint39-first-party-session-organizational-access-revalidation-regression.yml
          EOF
          sort -o /tmp/sprint40-source-preservation-paths /tmp/sprint40-source-preservation-paths
          test "$(sha256sum /tmp/sprint40-source-preservation-paths | awk '{print $1}')" = '671946562a3eef1375d87b70936bac263aa18f660d6ea274406ed57f1c653351'

          cat > /tmp/sprint40-source-paths <<'EOF'
          .github/workflows/sprint40-first-party-session-identity-disablement-revalidation-regression.yml
          apps/web/app/Application/Identity/FirstPartyIdentityEligibilityVerifier.php
          apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php
          apps/web/app/Infrastructure/Identity/LaravelFirstPartyIdentityEligibilityVerifier.php
          apps/web/app/Providers/AppServiceProvider.php
          apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php
          apps/web/tests/first-party-session-identity-disablement-revalidation.php
          docs/FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_FOUNDATION.md
          EOF
          sort -o /tmp/sprint40-source-paths /tmp/sprint40-source-paths
          test "$(sha256sum /tmp/sprint40-source-paths | awk '{print $1}')" = 'a9caf2b68210a38687fee543256aec04dc1e67ee1ef403608f7db69139957ff8'

          echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=false' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=false' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT46_JRN006_SOURCE_SUCCESSOR=false' >> "$GITHUB_ENV"
          git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" | sort > /tmp/actual-sprint39-source-paths
          if [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 6 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '6e0a346e98e8f622081c3d9e81866e8b44b79af800a178568bb71e3e6e28da62' ]]; then
            echo 'Recognized exact Sprint46 JRN-006 M7.4 compatibility predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 4 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'a5d3b0816c3b0e6f1fa7dd5ece73ea6347042372dd517af31e303c41d49abfaa' ]]; then
            echo 'Recognized exact four-workflow Sprint46 JRN-006 legacy-runner correction shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 14 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'ed29b6128c193f0efd6359748e220a37aefaec856acc4bc3b90f445ce3ccb674' ]]; then
            echo 'Recognized exact Sprint46 JRN-006 durable sale source successor shape.'
            echo 'ONEQAY_SPRINT46_JRN006_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif cmp -s /tmp/sprint39-source-paths /tmp/actual-sprint39-source-paths; then
            test "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" = '2cfc92c34f46375b11bf3fe92f9094cefa598d234133847fdd6629be211f12c4'
            echo 'Recognized exact Sprint39 source envelope.'
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 3 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '749ce1548b3491572af9dce61b34012842c638e409d8a9bbfd9625b94d8dd886' ]]; then
            echo 'Recognized exact Sprint46 JRN-006 three-workflow source-compatibility predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 2 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'c333c5a6ac31588aa9b0520160a1a1080a22af2971658d742ba9456eff94cbc8' ]]; then
            echo 'Recognized exact Sprint35/Sprint36 two-workflow compatibility predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 2 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'bde0c14f6b8c45f090844b9e5d701415690e990b8b7bea3b605f4ba04c727116' ]]; then
            echo 'Recognized exact Sprint33/Sprint34 two-workflow compatibility correction across canonical migration sixteen.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif cmp -s /tmp/post-sprint39-reconciliation-preservation-paths /tmp/actual-sprint39-source-paths; then
            grep -Fq '261f147d880a37fe9137b28d9541724c6c5e3987138f3fb56d5465ff31dc7412' .github/workflows/sprint35-privileged-totp-recovery-regression.yml
            grep -Fq '261f147d880a37fe9137b28d9541724c6c5e3987138f3fb56d5465ff31dc7412' .github/workflows/sprint37-first-party-all-session-termination-regression.yml
            echo 'Recognized exact post-Sprint41 five-workflow documentation-preservation predecessor shape with Sprint32 YAML closure compatibility.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif cmp -s /tmp/m7-5-sprint39-seven-workflow-preservation-paths /tmp/actual-sprint39-source-paths; then
            echo 'Recognized exact Sprint39 seven-workflow M7.5 preservation correction shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 13 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'b129d4b1c1135f2f5aecd5dde5ff1b5f6392eecb4d54006e80fc71889763647d' ]]; then
            echo 'Recognized exact Sprint40 canonical documentation synchronization successor shape.'
          elif cmp -s /tmp/sprint40-entry-gate-paths /tmp/actual-sprint39-source-paths; then
            echo 'Recognized exact Sprint40 identity-disablement revalidation entry-gate successor shape.'
          elif cmp -s /tmp/sprint41-entry-gate-paths /tmp/actual-sprint39-source-paths; then
            echo 'Recognized exact Sprint41 identity-authentication eligibility administration entry-gate successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '4297e429d072432122534ac88c96d3ce18ed870e31c2b5605d6fbb7d5ada0b0b' ]]; then
            echo 'Recognized exact Sprint42 disabled-identity session-termination entry-gate successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '2c0f9ad3b6e8a9fe2199202656da55c8af533c57cd653be61c28cebfeabb3dc2' ]]; then
            echo 'Recognized exact Sprint43 identity-authentication eligibility reactivation entry-gate successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '93559a76a6b31068316959787d8e7ebf041355955aa4226262958c39f791af89' ]]; then
            echo 'Recognized exact Sprint44 identity-reactivation fresh-authentication re-entry entry-gate successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '71ea234d8aa6e4691e5871e2165a61c162a04d30c824a6ae8b3aafb8c39962be' ]]; then
            echo 'Recognized exact Sprint43 identity-authentication eligibility reactivation schema/source-envelope gate successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'ea64d8bb82faab74b21dfc3cab1c5ac6550e4444fc91baee7567f4fe2680bd26' ]]; then
            echo 'Recognized exact Sprint42 disabled-identity session-termination schema/source-envelope gate successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif cmp -s /tmp/sprint40-schema-source-envelope-gate-paths /tmp/actual-sprint39-source-paths; then
            echo 'Recognized exact Sprint40 identity-disablement revalidation schema/source-envelope gate successor shape.'
          elif cmp -s /tmp/sprint41-schema-source-envelope-gate-paths /tmp/actual-sprint39-source-paths; then
            echo 'Recognized exact Sprint41 identity-authentication eligibility administration schema/source-envelope gate successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif cmp -s /tmp/sprint40-source-preservation-paths /tmp/actual-sprint39-source-paths; then
            grep -Fq 'bde0c14f6b8c45f090844b9e5d701415690e990b8b7bea3b605f4ba04c727116' .github/workflows/sprint33-recovery-bound-password-reset-regression.yml
            grep -Fq 'bde0c14f6b8c45f090844b9e5d701415690e990b8b7bea3b605f4ba04c727116' .github/workflows/sprint34-authenticated-password-change-regression.yml
            echo 'Recognized exact Sprint40 six-workflow source-preservation predecessor shape with historical correction closure.'
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 19 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'fc2b578fc9d090dfce1fd34c5acfa7a07f065979a94f62becb3dc54dee664920' ]]; then
            echo 'Recognized exact post-Sprint41 19-workflow historical preservation correction shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 7 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'f3f8b3ad0cca378307000ca242c4b9a4a8a7ab967d1649a5cd6408837001364c' ]]; then
            echo 'Recognized exact Sprint41 seven-workflow source-preservation predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 12 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'b2c5fc10a8baa2d56991d6dbd36b0407159d70953654ef322a9a11d23660489b' ]]; then
            echo 'Recognized exact Sprint41 identity-authentication eligibility administration source successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 8 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'e773cbe308d202808021c452c75954cc80234c081bbb1f23cb757210f76b8c85' ]]; then
            echo 'Recognized exact Sprint42 eight-workflow source-preservation predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 8 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '6315890d318c3cdfca549bfacef6cb8d1ca66a4421416b49b4978095a98b6729' ]]; then
            echo 'Recognized exact Sprint42 disabled-identity session-termination source successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 9 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'b8afa8415c0675c2675c1b8dbd528b44e8181a8f79a97fe28b0a0996623e1749' ]]; then
            echo 'Recognized exact Sprint43 nine-workflow source-preservation predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 2 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '4eb9f6b88fe36860855f16eff81c962c37d91aab5e73e445dca87749ea388055' ]]; then
            echo 'Recognized exact Sprint50 Sprint36/Sprint39 two-workflow historical-compatibility predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 9 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == 'c7f53821f0f3fc1554ee23a3c85b53369972accb0815ec2b8096664054cd01b9' ]]; then
            echo 'Recognized exact Sprint49 nine-workflow historical compatibility predecessor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif [[ "$(wc -l < /tmp/actual-sprint39-source-paths)" -eq 9 ]] && [[ "$(sha256sum /tmp/actual-sprint39-source-paths | awk '{print $1}')" == '3d0293362f451fe4bf472d0d2c38c3eec3d67df75b451ba5b273e8dbdb0f2eed' ]]; then
            echo 'Recognized exact Sprint43 identity-authentication eligibility reactivation source successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          elif cmp -s /tmp/sprint40-source-paths /tmp/actual-sprint39-source-paths; then
            echo 'Recognized exact Sprint40 identity-disablement revalidation source successor shape.'
            echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
          else
            echo 'Sprint39 changed-file envelope mismatch.' >&2
            diff -u /tmp/sprint39-source-paths /tmp/actual-sprint39-source-paths >&2 || true
            exit 1
          fi

      - name: Isolate Sprint41 journal from historical Sprint39 executable preservation
        if: env.ONEQAY_SPRINT41_SOURCE_SUCCESSOR == 'true'
        shell: bash
        run: |
          set -euo pipefail
          migrations=(
            'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
            'apps/web/database/migrations/0000_00_00_000016_create_pos_sale_completion_foundation.php'
            'apps/web/database/migrations/0000_00_00_000017_create_pos_catalog_preparation_journal.php'
            'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php'
          )
          for migration in "${migrations[@]}"; do
            if [[ -f "$migration" ]]; then
              git update-index --assume-unchanged "$migration"
              rm -f "$migration"
            fi
          done

      - name: Enforce NO_SCHEMA_CHANGE and migration immutability
        if: github.event_name == 'pull_request' && env.ONEQAY_S39_JRN004_COMPATIBILITY != 'true'
        shell: bash
        run: |
          set -euo pipefail
          base="${{ github.event.pull_request.base.sha }}"
          head="${{ github.event.pull_request.head.sha }}"
          if [[ "${ONEQAY_SPRINT46_JRN006_SOURCE_SUCCESSOR:-false}" == 'true' ]]; then
            git diff --exit-code "$base" "$head" -- \
              apps/web/database/migrations \
              ':(exclude)apps/web/database/migrations/0000_00_00_000016_create_pos_sale_completion_foundation.php'
          elif [[ "${ONEQAY_SPRINT41_SOURCE_SUCCESSOR:-false}" == 'true' ]]; then
            git diff --exit-code "$base" "$head" -- \
              apps/web/database/migrations \
              ':(exclude)apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
          elif [[ "${ONEQAY_SPRINT40_SOURCE_SUCCESSOR:-false}" == 'true' ]]; then
            git diff --exit-code "$base" "$head" -- \
              apps/web/database/migrations \
              ':(exclude)apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
          else
            git diff --exit-code "$base" "$head" -- apps/web/database/migrations
          fi
          mapfile -t migrations < <(find apps/web/database/migrations -maxdepth 1 -type f -name '*.php' -printf '%f\n' | sort)
          if [[ "${ONEQAY_SPRINT40_SOURCE_SUCCESSOR:-false}" == 'true' ]]; then
            test "${#migrations[@]}" -eq 14
            test "$(printf '%s\n' "${migrations[@]}" | grep -c '^0000_00_00_000014_add_first_party_authentication_eligibility_to_identities\.php$')" -eq 1
          else
            test "${#migrations[@]}" -eq 13
            ! printf '%s\n' "${migrations[@]}" | grep -q '^0000_00_00_000014_'
          fi
          for index in $(seq 1 13); do
            prefix="$(printf '0000_00_00_%06d_' "$index")"
            test "$(printf '%s\n' "${migrations[@]}" | grep -c "^${prefix}")" -eq 1
          done

      - name: Preserve excluded authority and public contract surfaces
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          base="${{ github.event.pull_request.base.sha }}"
          head="${{ github.event.pull_request.head.sha }}"
          if [[ "${ONEQAY_SPRINT41_SOURCE_SUCCESSOR:-false}" == 'true' ]]; then
            git diff --exit-code "$base" "$head" -- \
              apps/web/app/Application/Identity/FirstPartySessionAuthorityService.php \
              apps/web/app/Application/Identity/FirstPartySessionAuthorityRepository.php \
              apps/web/app/Infrastructure/Identity/LaravelFirstPartySessionAuthorityRepository.php \
              apps/web/app/Application/Tenancy/TenantMembershipVerifier.php \
              apps/web/app/Infrastructure/Tenancy/LaravelTenantMembershipVerifier.php \
              apps/web/app/Application/Organization/OrganizationalRelationshipVerifier.php \
              apps/web/app/Infrastructure/Organization/LaravelOrganizationalRelationshipVerifier.php \
              apps/web/app/Application/Access/DurableOrganizationalAccessRepository.php \
              apps/web/app/Infrastructure/Access/LaravelDurableOrganizationalAccessRepository.php \
              apps/web/bootstrap/app.php \
              apps/web/config/oneqay.php \
              apps/web/tests/run.php \
              apps/web/composer.json \
              apps/web/composer.lock \
              apps/web/package.json \
              apps/web/package-lock.json
          elif [[ "${ONEQAY_SPRINT40_SOURCE_SUCCESSOR:-false}" == 'true' ]]; then
            git diff --exit-code "$base" "$head" -- \
              apps/web/app/Application/Identity/FirstPartySessionAuthorityService.php \
              apps/web/app/Application/Identity/FirstPartySessionAuthorityRepository.php \
              apps/web/app/Infrastructure/Identity/LaravelFirstPartySessionAuthorityRepository.php \
              apps/web/app/Application/Tenancy/TenantMembershipVerifier.php \
              apps/web/app/Infrastructure/Tenancy/LaravelTenantMembershipVerifier.php \
              apps/web/app/Application/Organization/OrganizationalRelationshipVerifier.php \
              apps/web/app/Infrastructure/Organization/LaravelOrganizationalRelationshipVerifier.php \
              apps/web/app/Application/Access/DurableOrganizationalAccessRepository.php \
              apps/web/app/Infrastructure/Access/LaravelDurableOrganizationalAccessRepository.php \
              apps/web/bootstrap/app.php \
              apps/web/config/oneqay.php \
              apps/web/routes/web.php \
              apps/web/tests/run.php \
              apps/web/composer.json \
              apps/web/composer.lock \
              apps/web/package.json \
              apps/web/package-lock.json
          else
            git diff --exit-code "$base" "$head" -- \
              apps/web/app/Application/Identity/FirstPartySessionAuthorityService.php \
              apps/web/app/Application/Identity/FirstPartySessionAuthorityRepository.php \
              apps/web/app/Infrastructure/Identity/LaravelFirstPartySessionAuthorityRepository.php \
              apps/web/app/Application/Tenancy/TenantMembershipVerifier.php \
              apps/web/app/Infrastructure/Tenancy/LaravelTenantMembershipVerifier.php \
              apps/web/app/Application/Organization/OrganizationalRelationshipVerifier.php \
              apps/web/app/Infrastructure/Organization/LaravelOrganizationalRelationshipVerifier.php \
              apps/web/app/Application/Access/DurableOrganizationalAccessRepository.php \
              apps/web/app/Infrastructure/Access/LaravelDurableOrganizationalAccessRepository.php \
              apps/web/app/Providers/AppServiceProvider.php \
              apps/web/bootstrap/app.php \
              apps/web/config/oneqay.php \
              apps/web/routes/web.php \
              apps/web/tests/run.php \
              apps/web/composer.json \
              apps/web/composer.lock \
              apps/web/package.json \
              apps/web/package-lock.json
          fi

      - name: Validate exact request-time revalidation wiring
        shell: bash
        run: |
          set -euo pipefail
          middleware='apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php'
          grep -Fq 'TenantMembershipVerifier $tenantMemberships' "$middleware"
          grep -Fq 'OrganizationalRelationshipVerifier $organizationalRelationships' "$middleware"
          grep -Fq '$this->sessionAuthorities->assertActiveCurrent(' "$middleware"
          grep -Fq '$this->tenantMemberships->verify($identityId->value(), $tenantId->value())' "$middleware"
          grep -Fq '$this->organizationalRelationships->verify(' "$middleware"
          grep -Fq 'OrganizationId::fromString($organization)' "$middleware"
          grep -Fq 'OutletId::fromString($outlet)' "$middleware"
          grep -Fq 'DeviceId::fromString($device)' "$middleware"
          grep -Fq '$this->assertCanonicalIdentifier($tenant, $tenantId->value())' "$middleware"
          grep -Fq '$this->assertCanonicalIdentifier($identity, $identityId->value())' "$middleware"
          grep -Fq "throw new InvalidArgumentException('Device-bound session authority requires an outlet.')" "$middleware"
          grep -Fq "SafeErrorEnvelope::make('SESSION_AUTHORITY_DENIED'" "$middleware"
          grep -Fq "env('ONEQAY_AUTHENTICATION_SESSION_CONTROL_ENABLED', false)" apps/web/config/oneqay.php
          grep -Fq "'idle_ttl_seconds' => 7200" apps/web/config/oneqay.php
          grep -Fq "'absolute_ttl_seconds' => 43200" apps/web/config/oneqay.php

      - name: Preserve route and audit vocabulary
        shell: bash
        run: |
          set -euo pipefail
          repo='apps/web/app/Infrastructure/Identity/LaravelFirstPartySessionAuthorityRepository.php'
          for event in session_issued session_revoked other_sessions_revoked all_sessions_revoked session_logout; do
            grep -Fq "'$event'" "$repo"
          done
          ! grep -RInE 'organizational_access_(revoked|denied)|session_org|org_access_revoked' \
            apps/web/app/Infrastructure/Identity \
            apps/web/routes/web.php
          grep -Fq "Route::get('/auth/sessions'" apps/web/routes/web.php
          grep -Fq "Route::post('/auth/sessions/revoke-all'" apps/web/routes/web.php

      - name: Lint Sprint39 source
        shell: bash
        run: |
          set -euo pipefail
          php -l apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php
          php -l apps/web/tests/first-party-session-organizational-access-revalidation.php

      - name: Install locked application dependencies
        working-directory: apps/web
        run: |
          composer validate --no-check-publish
          composer install --no-interaction --no-progress --prefer-dist

      - name: Run dedicated Sprint39 organizational access regression
        working-directory: apps/web
        shell: bash
        env:
          ONEQAY_PERSISTENCE_ENABLED: 'true'
        run: |
          set -euo pipefail
          migration='database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
          middleware='app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php'
          isolated=false
          restore_sprint40_fixture() {
            if [[ "$isolated" == 'true' ]]; then
              mv /tmp/oneqay-s39-s40-migration.php "$migration"
              mv /tmp/oneqay-s39-s40-middleware.php "$middleware"
              isolated=false
            fi
          }
          if [[ "${ONEQAY_SPRINT40_SOURCE_SUCCESSOR:-false}" == 'true' ]]; then
            cp "$migration" /tmp/oneqay-s39-s40-migration.php
            cp "$middleware" /tmp/oneqay-s39-s40-middleware.php
            rm "$migration"
            git show "bd55f92015d963379dcac3804a25f4d13ae55613:apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php" > "$middleware"
            isolated=true
          fi
          set +e
          output="$(php tests/first-party-session-organizational-access-revalidation.php 2>&1)"
          status=$?
          set -e
          restore_sprint40_fixture
          if (( status != 0 )); then
            printf '%s\n' "$output"
            exit "$status"
          fi
          printf '%s\n' "$output"
          grep -Fq 'Sprint39 first-party session organizational access revalidation regression passed.' <<<"$output"

      - name: Preserve Sprint36 session inventory and revocation
        working-directory: apps/web
        run: php tests/first-party-session-inventory-revocation.php

      - name: Preserve Sprint37 all-session termination
        working-directory: apps/web
        run: php tests/first-party-session-all-session-termination.php

      - name: Preserve Sprint38 absolute lifetime
        working-directory: apps/web
        env:
          ONEQAY_PERSISTENCE_ENABLED: 'true'
        run: php tests/first-party-session-absolute-lifetime.php

      - name: Preserve full application regression
        working-directory: apps/web
        run: php tests/run.php

      - name: Preserve protected runtime boundaries
        shell: bash
        run: |
          set -euo pipefail
          grep -Fq 'Technical Preview remains **NO_SCHEMA_CHANGE**' docs/FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_FOUNDATION.md
          grep -Fq 'Production remains **NO-GO / NOT AUTHORIZED**' docs/FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_FOUNDATION.md
          grep -Fq 'Updater remains **DISABLED / UNWIRED**' docs/FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_FOUNDATION.md
          grep -Fq 'Local / Test / CI' docs/FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_FOUNDATION.md
          if grep -RInE 'ONEQAY_TECHNICAL_PREVIEW_ENABLED[[:space:]]*=[[:space:]]*true|runtime_class.*production.*session_control' \
            apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php \
            apps/web/config/oneqay.php; then
            echo 'Sprint39 activated a protected runtime.' >&2
            exit 1
          fi

      - name: Confirm tracked source cleanliness
        run: git diff --exit-code
