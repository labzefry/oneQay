name: Sprint167 POS Active Shift Performance Workspace Regression

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sprint167-pos-active-shift-performance-${{ github.event.pull_request.number || github.ref }}-${{ github.event.pull_request.head.sha || github.sha }}
  cancel-in-progress: true

jobs:
  qualify-sprint167-pos-active-shift-performance:
    runs-on: ubuntu-latest
    env:
      APP_NAME: oneQay
      APP_ENV: testing
      APP_KEY: base64:YWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWE=
      APP_DEBUG: "false"
      APP_URL: http://localhost
      ONEQAY_RUNTIME_CLASS: ci
      SESSION_DRIVER: array
      CACHE_STORE: array
    steps:
      - name: Checkout exact source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Qualify exact Sprint167 engineering envelope
        if: github.event_name == 'pull_request'
        env:
          EXPECTED_PATH_HASH: e9ca990e36ce896428bc749d19cf47025ce0525fd707f5e330d5d1cfbbfc8b48
          EXPECTED_PATH_COUNT: "12"
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${{ github.base_ref }}"
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | LC_ALL=C sort > /tmp/s167-paths.txt
          test "$(wc -l < /tmp/s167-paths.txt | tr -d ' ')" = "$EXPECTED_PATH_COUNT"
          test "$(sha256sum /tmp/s167-paths.txt | awk '{print $1}')" = "$EXPECTED_PATH_HASH"
          cat > /tmp/s167-expected.txt <<'EOF'
          .github/workflows/sprint167-pos-active-shift-performance-workspace-regression.yml
          apps/web/app/Application/Pos/PosActiveShiftPerformanceWorkspaceRepository.php
          apps/web/app/Application/Pos/PosActiveShiftPerformanceWorkspaceSnapshot.php
          apps/web/app/Application/Pos/ViewPosActiveShiftPerformanceWorkspace.php
          apps/web/app/Delivery/Http/Pos/PosActiveShiftPerformanceWorkspaceController.php
          apps/web/app/Delivery/Http/Pos/PosOperationsHubController.php
          apps/web/app/Infrastructure/Pos/LaravelPosActiveShiftPerformanceWorkspaceRepository.php
          apps/web/app/Providers/PosActiveShiftPerformanceWorkspaceServiceProvider.php
          apps/web/app/Providers/PosOperationsHubServiceProvider.php
          apps/web/config/pos_active_shift_performance.php
          apps/web/resources/js/pages/Pos/ActiveShiftPerformance.vue
          apps/web/tests/pos-active-shift-performance-workspace.php
          EOF
          diff -u /tmp/s167-expected.txt /tmp/s167-paths.txt
          git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- apps/web/app/Application/Authorization/PosPermission.php
          git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- apps/web/app/Application/Pos/CompleteSale.php
          git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- apps/web/app/Infrastructure/Pos/LaravelDurablePosSaleRepository.php
          git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- apps/web/app/Infrastructure/Pos/LaravelExpectedCashSnapshotReader.php
          git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- apps/web/app/Infrastructure/Pos/LaravelCloseShiftRepository.php
          git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- apps/web/routes/web.php apps/web/bootstrap/providers.php
          git diff --quiet "origin/${{ github.base_ref }}...HEAD" -- apps/web/database/migrations apps/web/database/module-migrations

      - name: Preserve operational NO-GO
        run: |
          set -euo pipefail
          test "$(jq -r '.migration27.state' ops/final-shift-close/STATE.json)" = NOT_EXECUTED
          test "$(jq -r '.permission_provisioning.state' ops/final-shift-close/STATE.json)" = NONE
          test "$(jq -r '.feature_activation.state' ops/final-shift-close/STATE.json)" = INACTIVE
          test "$(jq -r '.deployment_authority' ops/final-shift-close/STATE.json)" = NOT_GRANTED
          test "$(jq -r '.technical_preview_activation' ops/final-shift-close/STATE.json)" = NOT_AUTHORIZED
          test "$(jq -r '.production_activation' ops/final-shift-close/STATE.json)" = NOT_AUTHORIZED
          test "$(jq -r '.updater_activation' ops/final-shift-close/STATE.json)" = INACTIVE
          test "$(jq -r '.selection_state' ops/final-shift-close/DURABLE_ACTIVATION_TARGET_SELECTION.json)" = BLOCKED_NO_QUALIFIED_NON_SYNTHETIC_DURABLE_TARGET
          test "$(jq -r '.selected_target' ops/final-shift-close/DURABLE_ACTIVATION_TARGET_SELECTION.json)" = null

      - name: Preserve read-only active shift performance contract
        run: |
          set -euo pipefail
          grep -Fq "pos.reporting.sales-summary.view" apps/web/app/Application/Pos/ViewPosActiveShiftPerformanceWorkspace.php
          grep -Fq "whereNull('shift_id')" apps/web/app/Infrastructure/Pos/LaravelPosActiveShiftPerformanceWorkspaceRepository.php
          grep -Fq "oneqay_pos_shifts" apps/web/app/Infrastructure/Pos/LaravelPosActiveShiftPerformanceWorkspaceRepository.php
          grep -Fq "oneqay_pos_sales" apps/web/app/Infrastructure/Pos/LaravelPosActiveShiftPerformanceWorkspaceRepository.php
          grep -Fq "oneqay_pos_sale_voids" apps/web/app/Infrastructure/Pos/LaravelPosActiveShiftPerformanceWorkspaceRepository.php
          grep -Fq "oneqay_pos_sale_cash_refunds" apps/web/app/Infrastructure/Pos/LaravelPosActiveShiftPerformanceWorkspaceRepository.php
          ! grep -Eq -- '->(insert|update|delete|increment|decrement)\(' apps/web/app/Infrastructure/Pos/LaravelPosActiveShiftPerformanceWorkspaceRepository.php
          grep -Fq "ONEQAY_POS_ACTIVE_SHIFT_PERFORMANCE_ENABLED" apps/web/config/pos_active_shift_performance.php
          grep -Fq "PosActiveShiftPerformanceWorkspaceServiceProvider::class" apps/web/app/Providers/PosOperationsHubServiceProvider.php
          grep -Fq "pos.reporting.active-shift-performance" apps/web/app/Providers/PosActiveShiftPerformanceWorkspaceServiceProvider.php
          grep -Fq "pos.reporting.active-shift-performance" apps/web/app/Delivery/Http/Pos/PosOperationsHubController.php
          grep -Fq 'Route::has($routeName)' apps/web/app/Delivery/Http/Pos/PosOperationsHubController.php

      - name: Install PHP dependencies
        working-directory: apps/web
        run: |
          composer validate --no-check-publish
          composer install --no-interaction --no-progress --prefer-dist

      - name: Validate PHP syntax
        working-directory: apps/web
        run: |
          find app config database routes tests -name '*.php' -print0 | xargs -0 -n1 php -l >/tmp/php-lint.log
          cat /tmp/php-lint.log

      - name: Execute canonical and POS regressions
        working-directory: apps/web
        run: |
          composer test
          php tests/pos-operational-reporting.php
          php tests/pos-cashier-workspace.php
          php tests/pos-shift-start-workspace.php
          php tests/pos-sale-correction-workspace.php
          php tests/pos-sale-history-workspace.php
          php tests/pos-catalog-inventory-setup-workspace.php
          php tests/pos-operations-hub.php
          php tests/pos-cash-variance-reconciliation-workspace.php
          php tests/pos-inventory-replenishment-workspace.php
          php tests/pos-inventory-accountability-workspace.php
          php tests/pos-product-sales-performance-workspace.php
          php tests/pos-active-shift-performance-workspace.php

      - name: Setup exact Node LTS
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 24.19.0

      - name: Type-check and build active shift performance UI
        working-directory: apps/web
        run: |
          npm ci --ignore-scripts --no-fund
          npm run type-check
          npm run build

      - name: Verify tracked-source cleanliness
        run: |
          git diff --exit-code
          test -z "$(git status --porcelain)"
