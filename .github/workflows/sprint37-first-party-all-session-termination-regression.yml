# Sprint44 Sprint33/Sprint34 exact compatibility isolation predecessor anchor.
# Sprint44 source four-path compatibility predecessor anchor.
# Sprint41 Sprint38 source-successor route-guard correction anchor.
# Sprint41 Sprint38 legacy-horizon executable-isolation correction anchor.
# Sprint41 Sprint38 legacy-horizon migration-state correction anchor.
# Sprint41 schema-gate Sprint38 canonical-state correction anchor.
# Sprint41 schema/source gate exact-trigger preservation anchor.
# Post-merge M7.5 main-push oracle correction anchor.
# Post-Sprint40 M7.5 canonical-main fixture isolation anchor.
# Post-Sprint39 M7.5 seven-workflow preservation predecessor anchor.
# Sprint40 Sprint38/Sprint39 historical fixture isolation anchor.
# Sprint40 Sprint37 same-step successor-state correction anchor.
# Sprint40 Sprint38 same-step successor-state correction anchor.
# Sprint40 19-workflow preservation-horizon bridge anchor.
name: Sprint37 First-Party All-Session Termination Regression

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sprint37-all-session-termination-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  sprint37-all-session-termination:
    name: sprint37-all-session-termination
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      APP_NAME: oneQay
      APP_ENV: testing
      APP_KEY: base64:cnJycnJycnJycnJycnJycnJycnJycnJycnJycnJycnI=
      APP_DEBUG: "false"
      APP_URL: http://localhost
      ONEQAY_RUNTIME_CLASS: ci
      ONEQAY_PERSISTENCE_ENABLED: "false"
      ONEQAY_AUTHENTICATION_SESSION_CONTROL_ENABLED: "false"
      SESSION_DRIVER: array
      CACHE_STORE: array
    steps:
      - name: Checkout exact source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Prepare frozen Sprint37 source manifests
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/sprint37-paths <<'EOF'
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          apps/web/app/Application/Identity/FirstPartySessionAuthorityRepository.php
          apps/web/app/Application/Identity/FirstPartySessionAuthorityService.php
          apps/web/app/Delivery/Http/Identity/FirstPartySessionControlController.php
          apps/web/app/Infrastructure/Identity/LaravelFirstPartySessionAuthorityRepository.php
          apps/web/routes/web.php
          apps/web/tests/first-party-session-all-session-termination.php
          apps/web/tests/first-party-session-inventory-revocation.php
          docs/FIRST_PARTY_ALL_SESSION_TERMINATION_FOUNDATION.md
          EOF
          sort -o /tmp/sprint37-paths /tmp/sprint37-paths
          test "$(sha256sum /tmp/sprint37-paths | awk '{print $1}')" = "a221779e05b1e8ab220610b5068be5d1eb01bc08b516b338ba8f22373e7d89d0"

          cat > /tmp/post-sprint37-preservation-paths <<'EOF'
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          EOF
          sort -o /tmp/post-sprint37-preservation-paths /tmp/post-sprint37-preservation-paths
          test "$(sha256sum /tmp/post-sprint37-preservation-paths | awk '{print $1}')" = "93fe19acad52a31981ecea81ab1dc65c730f0f01729959ca6eaedb0de5b128b7"

          cat > /tmp/post-sprint38-preservation-paths <<'EOF'
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          EOF
          sort -o /tmp/post-sprint38-preservation-paths /tmp/post-sprint38-preservation-paths
          test "$(sha256sum /tmp/post-sprint38-preservation-paths | awk '{print $1}')" = "f6064e6563ec987fc487162fc29285870fab4869509a626c1b9a9280dc6ebafe"

          cat > /tmp/post-sprint37-reconciliation-paths <<'EOF'
          docs/ai/AI_NEXT_TASK.md
          docs/ai/AI_POST_SPRINT_37_CANONICAL_STATE.md
          EOF
          sort -o /tmp/post-sprint37-reconciliation-paths /tmp/post-sprint37-reconciliation-paths
          test "$(sha256sum /tmp/post-sprint37-reconciliation-paths | awk '{print $1}')" = "2b3a711f5cb921dea0fb51a005c0bc742186bce526a0a7e87b10215733215dbe"

          cat > /tmp/post-sprint38-reconciliation-paths <<'EOF'
          docs/ai/AI_NEXT_TASK.md
          docs/ai/AI_POST_SPRINT_38_CANONICAL_STATE.md
          EOF
          sort -o /tmp/post-sprint38-reconciliation-paths /tmp/post-sprint38-reconciliation-paths
          test "$(sha256sum /tmp/post-sprint38-reconciliation-paths | awk '{print $1}')" = "e1bd5f1acb858e3b8e91f3b57dfe8505c4142564142bf9a32b98240c1dd9c472"

          cat > /tmp/post-sprint39-reconciliation-paths <<'EOF'
          docs/ai/AI_NEXT_TASK.md
          docs/ai/AI_POST_SPRINT_39_CANONICAL_STATE.md
          EOF
          sort -o /tmp/post-sprint39-reconciliation-paths /tmp/post-sprint39-reconciliation-paths
          test "$(sha256sum /tmp/post-sprint39-reconciliation-paths | awk '{print $1}')" = "f38b1a2dea2bd5a55a5cd166db0ea7b2d4ae5186cefe2cb5db27f18f9639e866"

          cat > /tmp/sprint38-entry-gate-paths <<'EOF'
          docs/SPRINT_38_FIRST_PARTY_SESSION_ABSOLUTE_LIFETIME_ENTRY_GATE.md
          EOF
          sort -o /tmp/sprint38-entry-gate-paths /tmp/sprint38-entry-gate-paths
          test "$(sha256sum /tmp/sprint38-entry-gate-paths | awk '{print $1}')" = "b5e631b39dee456c18e2f14ab3266db599a97d3b5f3778c8d8dfdd3243658434"

          cat > /tmp/sprint39-entry-gate-paths <<'EOF'
          docs/SPRINT_39_FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_ENTRY_GATE.md
          EOF
          sort -o /tmp/sprint39-entry-gate-paths /tmp/sprint39-entry-gate-paths
          test "$(sha256sum /tmp/sprint39-entry-gate-paths | awk '{print $1}')" = "b2c4936a3315788100b47537b32474e7079143942c38684cf2394628f1084dd2"

          cat > /tmp/sprint40-entry-gate-paths <<'EOF'
          docs/SPRINT_40_FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_ENTRY_GATE.md
          EOF
          sort -o /tmp/sprint40-entry-gate-paths /tmp/sprint40-entry-gate-paths
          test "$(sha256sum /tmp/sprint40-entry-gate-paths | awk '{print $1}')" = "7a8c362aa7ec66500ba544911e3fb008a20f5d21ac4d00d6a12ea04749ed2466"

          cat > /tmp/sprint41-entry-gate-paths <<'EOF'
          docs/SPRINT_41_FIRST_PARTY_IDENTITY_AUTHENTICATION_ELIGIBILITY_ADMINISTRATION_ENTRY_GATE.md
          EOF
          sort -o /tmp/sprint41-entry-gate-paths /tmp/sprint41-entry-gate-paths
          test "$(sha256sum /tmp/sprint41-entry-gate-paths | awk '{print $1}')" = "074a842bd6fb56ac06ae4658aeb6596b3674d426c4d58fee594b699820eac5f2"

          cat > /tmp/sprint40-schema-source-envelope-gate-paths <<'EOF'
          docs/SPRINT_40_FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_SCHEMA_SOURCE_ENVELOPE_GATE.md
          EOF
          sort -o /tmp/sprint40-schema-source-envelope-gate-paths /tmp/sprint40-schema-source-envelope-gate-paths
          test "$(sha256sum /tmp/sprint40-schema-source-envelope-gate-paths | awk '{print $1}')" = "9a3a85fb3de7ce007b09586af60e18b0cefd6ec1b6fba1b3c2b64e508354d803"

          cat > /tmp/sprint41-schema-source-envelope-gate-paths <<'EOF'
          docs/SPRINT_41_FIRST_PARTY_IDENTITY_AUTHENTICATION_ELIGIBILITY_ADMINISTRATION_SCHEMA_SOURCE_ENVELOPE_GATE.md
          EOF
          sort -o /tmp/sprint41-schema-source-envelope-gate-paths /tmp/sprint41-schema-source-envelope-gate-paths
          test "$(sha256sum /tmp/sprint41-schema-source-envelope-gate-paths | awk '{print $1}')" = "1a2c18a7ec9b363a9b2ef3bc6ff86366ab58f4812e8968d83642e6ce359f48b2"

          cat > /tmp/sprint40-source-preservation-paths <<'EOF'
          .github/workflows/m7-5-preview-release-artifact.yml
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          .github/workflows/sprint39-first-party-session-organizational-access-revalidation-regression.yml
          EOF
          sort -o /tmp/sprint40-source-preservation-paths /tmp/sprint40-source-preservation-paths
          test "$(sha256sum /tmp/sprint40-source-preservation-paths | awk '{print $1}')" = "671946562a3eef1375d87b70936bac263aa18f660d6ea274406ed57f1c653351"

          cat > /tmp/sprint40-source-paths <<'EOF'
          .github/workflows/sprint40-first-party-session-identity-disablement-revalidation-regression.yml
          apps/web/app/Application/Identity/FirstPartyIdentityEligibilityVerifier.php
          apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php
          apps/web/app/Infrastructure/Identity/LaravelFirstPartyIdentityEligibilityVerifier.php
          apps/web/app/Providers/AppServiceProvider.php
          apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php
          apps/web/tests/first-party-session-identity-disablement-revalidation.php
          docs/FIRST_PARTY_SESSION_IDENTITY_DISABLEMENT_REVALIDATION_FOUNDATION.md
          EOF
          sort -o /tmp/sprint40-source-paths /tmp/sprint40-source-paths
          test "$(sha256sum /tmp/sprint40-source-paths | awk '{print $1}')" = "a9caf2b68210a38687fee543256aec04dc1e67ee1ef403608f7db69139957ff8"

          cat > /tmp/sprint39-schema-source-envelope-gate-paths <<'EOF'
          docs/SPRINT_39_FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_SCHEMA_SOURCE_ENVELOPE_GATE.md
          EOF
          sort -o /tmp/sprint39-schema-source-envelope-gate-paths /tmp/sprint39-schema-source-envelope-gate-paths
          test "$(sha256sum /tmp/sprint39-schema-source-envelope-gate-paths | awk '{print $1}')" = "c66722c0e61aac85deb260fb5f8989aeae55cacdd61bb60ed9beadc161b570af"

          cat > /tmp/sprint39-source-paths <<'EOF'
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          .github/workflows/sprint39-first-party-session-organizational-access-revalidation-regression.yml
          apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php
          apps/web/tests/first-party-session-organizational-access-revalidation.php
          docs/FIRST_PARTY_SESSION_ORGANIZATIONAL_ACCESS_REVALIDATION_FOUNDATION.md
          EOF
          sort -o /tmp/sprint39-source-paths /tmp/sprint39-source-paths
          test "$(sha256sum /tmp/sprint39-source-paths | awk '{print $1}')" = "2cfc92c34f46375b11bf3fe92f9094cefa598d234133847fdd6629be211f12c4"

          cat > /tmp/sprint39-m75-preservation-correction-paths <<'EOF'
          .github/workflows/m7-5-preview-release-artifact.yml
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          EOF
          sort -o /tmp/sprint39-m75-preservation-correction-paths /tmp/sprint39-m75-preservation-correction-paths
          test "$(sha256sum /tmp/sprint39-m75-preservation-correction-paths | awk '{print $1}')" = "84031e95f99240ea9a4a6eb39f75ff8dfa731fa3f2506396642b811d3425cd68"

          cat > /tmp/sprint38-schema-source-envelope-gate-paths <<'EOF'
          docs/SPRINT_38_FIRST_PARTY_SESSION_ABSOLUTE_LIFETIME_SCHEMA_SOURCE_ENVELOPE_GATE.md
          EOF
          sort -o /tmp/sprint38-schema-source-envelope-gate-paths /tmp/sprint38-schema-source-envelope-gate-paths
          test "$(sha256sum /tmp/sprint38-schema-source-envelope-gate-paths | awk '{print $1}')" = "ce1f2c0f1f1aaa0bab782eab85f6b023e2749cd98835812288711d37117887f9"

          cat > /tmp/sprint38-historical-preservation-paths <<'EOF'
          .github/workflows/m7-2-tenant-isolation-regression.yml
          .github/workflows/m7-3-identity-org-context-regression.yml
          .github/workflows/m7-4a-technical-preview-interaction-regression.yml
          .github/workflows/m7-5-preview-db-qualification-regression.yml
          .github/workflows/m7-5-preview-release-artifact.yml
          .github/workflows/sprint21-role-permission-policy-regression.yml
          .github/workflows/sprint22-policy-administration-regression.yml
          .github/workflows/sprint23-initial-tenant-admin-provisioning-regression.yml
          .github/workflows/sprint24-protected-control-admin-lifecycle-regression.yml
          .github/workflows/sprint25-policy-administration-delivery-regression.yml
          .github/workflows/sprint26-identity-credential-verification-regression.yml
          .github/workflows/sprint27-first-party-session-establishment-regression.yml
          .github/workflows/sprint28-initial-password-enrollment-regression.yml
          .github/workflows/sprint29-first-control-principal-credential-bootstrap-regression.yml
          .github/workflows/sprint30-privileged-totp-mfa-regression.yml
          .github/workflows/sprint31-privileged-reauthentication-step-up-regression.yml
          .github/workflows/sprint33-recovery-bound-password-reset-regression.yml
          .github/workflows/sprint34-authenticated-password-change-regression.yml
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          EOF
          sort -o /tmp/sprint38-historical-preservation-paths /tmp/sprint38-historical-preservation-paths
          test "$(sha256sum /tmp/sprint38-historical-preservation-paths | awk '{print $1}')" = "fc2b578fc9d090dfce1fd34c5acfa7a07f065979a94f62becb3dc54dee664920"

          cat > /tmp/sprint38-source-paths <<'EOF'
          .github/workflows/sprint35-privileged-totp-recovery-regression.yml
          .github/workflows/sprint36-first-party-session-inventory-revocation-regression.yml
          .github/workflows/sprint37-first-party-all-session-termination-regression.yml
          .github/workflows/sprint38-first-party-session-absolute-lifetime-regression.yml
          apps/web/app/Application/Identity/FirstPartySessionAuthorityService.php
          apps/web/app/Providers/AppServiceProvider.php
          apps/web/config/oneqay.php
          apps/web/tests/first-party-session-absolute-lifetime.php
          apps/web/tests/first-party-session-inventory-revocation.php
          docs/FIRST_PARTY_SESSION_ABSOLUTE_LIFETIME_FOUNDATION.md
          EOF
          sort -o /tmp/sprint38-source-paths /tmp/sprint38-source-paths
          test "$(sha256sum /tmp/sprint38-source-paths | awk '{print $1}')" = "411950d5602dc7160668c88e08a3941ebccc8bdc82d20bee77ce4004f039d216"

      - name: Enforce frozen Sprint37 source envelope
        shell: bash
        run: |
          set -euo pipefail
          sprint40_source_successor=false
          sprint41_source_successor=false
          echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=false' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=false' >> "$GITHUB_ENV"
          if [[ "${{ github.event_name }}" = "pull_request" ]]; then
            git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" | sort > /tmp/actual-sprint37-paths
            if [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "261f147d880a37fe9137b28d9541724c6c5e3987138f3fb56d5465ff31dc7412" ]]; then
              if [[ -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php' ]]; then
                git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                rm -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                echo "Migration #14 isolated only from historical Sprint37 executable preservation."
              fi
              echo "Recognized exact Sprint32 workflow YAML closure successor shape."
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 5 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "ab09e044925ae98740a49c46a70ff387e5f5445b245d3a556d85ad6a354305ec" ]]; then
              if [[ -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php' ]]; then
                git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                rm -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                echo "Migration #14 isolated only from historical Sprint37 executable preservation."
              fi
              if [[ -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' ]]; then
                git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
                rm -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
                echo "Migration #15 isolated only from exact post-Sprint41 five-workflow documentation preservation."
              fi
              echo "Recognized exact post-Sprint41 five-workflow documentation-preservation predecessor shape."
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 3 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "fd5f1466a41a7b11f0684303f5e93196668c2ca929ce9119e415186ae6708e9d" ]]; then
              if [[ -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php' ]]; then
                git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                rm -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                echo "Migration #14 isolated only from historical Sprint37 executable preservation."
              fi
              if [[ -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' ]]; then
                git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
                rm -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
                echo "Migration #15 isolated only from exact Sprint43 legacy three-workflow compatibility correction."
              fi
              echo "Recognized exact Sprint43 legacy three-workflow compatibility correction shape."
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 13 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "b129d4b1c1135f2f5aecd5dde5ff1b5f6392eecb4d54006e80fc71889763647d" ]]; then
              echo "Recognized exact post-Sprint41 canonical documentation reconciliation shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 7 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "4784ffca1c940d3fa54a2a3988ead07e2de993bde8d3af2bd41014dbdf905be0" ]]; then
              echo "Recognized exact Sprint39 seven-workflow M7.5 preservation correction shape."
              sprint40_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 19 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "fc2b578fc9d090dfce1fd34c5acfa7a07f065979a94f62becb3dc54dee664920" ]]; then
              echo "Recognized exact post-Sprint41 19-workflow historical preservation correction shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 2 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "bde0c14f6b8c45f090844b9e5d701415690e990b8b7bea3b605f4ba04c727116" ]]; then
              if [[ -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php' ]]; then
                git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                rm -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
                echo "Migration #14 isolated only from exact Sprint33/Sprint34 historical compatibility preservation."
              fi
              if [[ -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' ]]; then
                git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
                rm -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
                echo "Migration #15 isolated only from exact Sprint33/Sprint34 historical compatibility preservation."
              fi
              echo "Recognized exact Sprint40 Sprint33/Sprint34 historical-compatibility correction shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint37-paths; then
              echo "Recognized exact Sprint37 source envelope."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/post-sprint37-preservation-paths; then
              echo "Recognized exact post-Sprint37 preservation predecessor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif cmp -s /tmp/actual-sprint37-paths /tmp/post-sprint38-preservation-paths; then
              echo "Recognized exact post-Sprint38 preservation predecessor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/post-sprint37-reconciliation-paths; then
              echo "Recognized exact post-Sprint37 canonical reconciliation successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/post-sprint38-reconciliation-paths; then
              echo "Recognized exact post-Sprint38 canonical reconciliation successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/post-sprint39-reconciliation-paths; then
              echo "Recognized exact post-Sprint39 canonical reconciliation successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint38-entry-gate-paths; then
              echo "Recognized exact Sprint38 entry-gate successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint39-entry-gate-paths; then
              echo "Recognized exact Sprint39 organizational-access revalidation entry-gate successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint40-entry-gate-paths; then
              echo "Recognized exact Sprint40 identity-disablement revalidation entry-gate successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint41-entry-gate-paths; then
              echo "Recognized exact Sprint41 identity-authentication eligibility administration entry-gate successor shape."
              sprint40_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "4297e429d072432122534ac88c96d3ce18ed870e31c2b5605d6fbb7d5ada0b0b" ]]; then
              echo "Recognized exact Sprint42 disabled-identity session-termination entry-gate successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "2c0f9ad3b6e8a9fe2199202656da55c8af533c57cd653be61c28cebfeabb3dc2" ]]; then
              echo "Recognized exact Sprint43 identity-authentication eligibility reactivation entry-gate successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "93559a76a6b31068316959787d8e7ebf041355955aa4226262958c39f791af89" ]]; then
              echo "Recognized exact Sprint44 identity-reactivation fresh-authentication re-entry entry-gate successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "5005a4ecc48fb122f024e09b3e73ab0ddada7a9d1e6aada6d2c53aae763e5975" ]]; then
              echo "Recognized exact Sprint44 identity-reactivation fresh-authentication re-entry schema/source-envelope gate successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 4 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "ff01f1355de6c7fdfd28c2d359eb70787dd8448f0b1fc6d9cb73c1a0fb76580a" ]]; then
              echo "Recognized exact Sprint44 identity-reactivation fresh-authentication re-entry source successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "71ea234d8aa6e4691e5871e2165a61c162a04d30c824a6ae8b3aafb8c39962be" ]]; then
              echo "Recognized exact Sprint43 identity-authentication eligibility reactivation schema/source-envelope gate successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 1 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "ea64d8bb82faab74b21dfc3cab1c5ac6550e4444fc91baee7567f4fe2680bd26" ]]; then
              echo "Recognized exact Sprint42 disabled-identity session-termination schema/source-envelope gate successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint40-schema-source-envelope-gate-paths; then
              echo "Recognized exact Sprint40 identity-disablement revalidation schema/source-envelope gate successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint41-schema-source-envelope-gate-paths; then
              echo "Recognized exact Sprint41 identity-authentication eligibility administration schema/source-envelope gate successor shape."
              sprint40_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint40-source-preservation-paths; then
              grep -Fq 'bde0c14f6b8c45f090844b9e5d701415690e990b8b7bea3b605f4ba04c727116' .github/workflows/sprint33-recovery-bound-password-reset-regression.yml
              grep -Fq 'bde0c14f6b8c45f090844b9e5d701415690e990b8b7bea3b605f4ba04c727116' .github/workflows/sprint34-authenticated-password-change-regression.yml
              echo "Recognized exact Sprint40 six-workflow source-preservation predecessor shape with historical correction closure."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint40-source-paths; then
              echo "Recognized exact Sprint40 identity-disablement revalidation source successor shape."
              sprint40_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 7 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "f3f8b3ad0cca378307000ca242c4b9a4a8a7ab967d1649a5cd6408837001364c" ]]; then
              echo "Recognized exact Sprint41 seven-workflow source-preservation predecessor shape."
              sprint40_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 12 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "b2c5fc10a8baa2d56991d6dbd36b0407159d70953654ef322a9a11d23660489b" ]]; then
              echo "Recognized exact Sprint41 identity-authentication eligibility administration source successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 8 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "e773cbe308d202808021c452c75954cc80234c081bbb1f23cb757210f76b8c85" ]]; then
              echo "Recognized exact Sprint42 eight-workflow source-preservation predecessor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 8 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "6315890d318c3cdfca549bfacef6cb8d1ca66a4421416b49b4978095a98b6729" ]]; then
              echo "Recognized exact Sprint42 disabled-identity session-termination source successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 9 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "b8afa8415c0675c2675c1b8dbd528b44e8181a8f79a97fe28b0a0996623e1749" ]]; then
              echo "Recognized exact Sprint43 nine-workflow source-preservation predecessor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif [[ "$(wc -l < /tmp/actual-sprint37-paths)" -eq 9 ]] && [[ "$(sha256sum /tmp/actual-sprint37-paths | awk '{print $1}')" == "3d0293362f451fe4bf472d0d2c38c3eec3d67df75b451ba5b273e8dbdb0f2eed" ]]; then
              echo "Recognized exact Sprint43 identity-authentication eligibility reactivation source successor shape."
              sprint40_source_successor=true
              sprint41_source_successor=true
              echo 'ONEQAY_SPRINT40_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
              echo 'ONEQAY_SPRINT41_SOURCE_SUCCESSOR=true' >> "$GITHUB_ENV"
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint39-schema-source-envelope-gate-paths; then
              echo "Recognized exact Sprint39 organizational-access revalidation schema/source-envelope gate successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint39-source-paths; then
              echo "Recognized exact Sprint39 organizational-access revalidation source successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint39-m75-preservation-correction-paths; then
              echo "Recognized exact Sprint39 M7.5 preservation correction shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint38-schema-source-envelope-gate-paths; then
              echo "Recognized exact Sprint38 schema/source-envelope gate successor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint38-historical-preservation-paths; then
              echo "Recognized exact Sprint38 19-path historical preservation predecessor shape."
            elif cmp -s /tmp/actual-sprint37-paths /tmp/sprint38-source-paths; then
              echo "Recognized exact Sprint38 source successor shape."
            else
              echo "Sprint37 changed-file envelope mismatch." >&2
              diff -u /tmp/sprint37-paths /tmp/actual-sprint37-paths >&2 || true
              exit 1
            fi

            if [[ "$sprint41_source_successor" == 'true' ]]; then
              unexpected_migrations="$(git diff --name-only \"${{ github.event.pull_request.base.sha }}\" \"${{ github.event.pull_request.head.sha }}\" -- apps/web/database/migrations/ | grep -v '^apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal\.php$' || true)"
              test -z "$unexpected_migrations" || { echo "Sprint37 Sprint41 successor mutated an unauthorized migration:" >&2; echo "$unexpected_migrations" >&2; exit 1; }
            elif [[ "$sprint40_source_successor" == 'true' ]]; then
              unexpected_migrations="$(git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" -- apps/web/database/migrations/ | grep -v '^apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities\.php$' || true)"
              test -z "$unexpected_migrations" || { echo "Sprint37 successor mutated an unauthorized migration:" >&2; echo "$unexpected_migrations" >&2; exit 1; }
            elif git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" -- apps/web/database/migrations/ | grep -q .; then
              echo "Sprint37 is NO_SCHEMA_CHANGE; migration mutation is forbidden." >&2
              exit 1
            fi
          fi

          mapfile -t migrations < <(find apps/web/database/migrations -maxdepth 1 -type f -name '*.php' -printf '%f\n' | sort)
          if [[ "$sprint41_source_successor" == 'true' ]]; then
            test "${#migrations[@]}" -eq 15
            test "$(printf '%s\n' "${migrations[@]}" | grep -c '^0000_00_00_000014_add_first_party_authentication_eligibility_to_identities\.php$')" -eq 1
            test "$(printf '%s\n' "${migrations[@]}" | grep -c '^0000_00_00_000015_create_identity_authentication_eligibility_administration_journal\.php$')" -eq 1
          elif [[ "$sprint40_source_successor" == 'true' ]]; then
            test "${#migrations[@]}" -eq 14
            test "$(printf '%s\n' "${migrations[@]}" | grep -c '^0000_00_00_000014_add_first_party_authentication_eligibility_to_identities\.php$')" -eq 1
          else
            test "${#migrations[@]}" -eq 13
            ! printf '%s\n' "${migrations[@]}" | grep -q '^0000_00_00_000014_'
          fi
          for index in $(seq 1 13); do
            prefix="$(printf '0000_00_00_%06d_' "$index")"
            count="$(printf '%s\n' "${migrations[@]}" | grep -c "^${prefix}")"
            test "$count" -eq 1
          done

      - name: Isolate Sprint41 journal from historical Sprint37 executable preservation
        if: env.ONEQAY_SPRINT41_SOURCE_SUCCESSOR == 'true'
        shell: bash
        run: |
          set -euo pipefail
          migration='apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
          if [[ -f "$migration" ]]; then
            git update-index --assume-unchanged "$migration"
            rm -f "$migration"
          fi

      - name: Validate Sprint37 source and schema boundary
        shell: bash
        run: |
          set -euo pipefail
          test -f apps/web/database/migrations/0000_00_00_000013_create_first_party_session_authority.php
          ! test -e apps/web/database/migrations/0000_00_00_000014_create_first_party_session_authority.php
          grep -F "env('ONEQAY_AUTHENTICATION_SESSION_CONTROL_ENABLED', false)" apps/web/config/oneqay.php
          grep -F "'idle_ttl_seconds' => 7200" apps/web/config/oneqay.php
          grep -F "'absolute_ttl_seconds' => 43200" apps/web/config/oneqay.php
          grep -F 'TenantMembershipVerifier $tenantMemberships' apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php
          grep -F 'OrganizationalRelationshipVerifier $organizationalRelationships' apps/web/app/Delivery/Http/Middleware/EnforceActiveFirstPartySessionAuthorityMiddleware.php
          grep -F "Route::post('/auth/sessions/revoke-all'" apps/web/routes/web.php
          grep -F -- "->name('auth.sessions.revoke-all');" apps/web/routes/web.php
          grep -F "'all_sessions_revoked'" apps/web/app/Infrastructure/Identity/LaravelFirstPartySessionAuthorityRepository.php
          grep -F "private const FRESHNESS_SECONDS = 300;" apps/web/app/Delivery/Http/Middleware/RequireFirstPartySessionControlMutationContextMiddleware.php

      - name: Validate PHP runtime and syntax
        shell: bash
        run: |
          set -euo pipefail
          php -r 'if (PHP_VERSION_ID < 80200 || PHP_VERSION_ID >= 80600) { exit(1); }'
          find apps/web/app apps/web/database/migrations apps/web/routes apps/web/tests -type f -name '*.php' -print0 |
            while IFS= read -r -d '' file; do php -l "$file"; done

      - name: Install locked Composer dependencies
        working-directory: apps/web
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Run Sprint37 all-session termination regression
        working-directory: apps/web
        run: php tests/first-party-session-all-session-termination.php

      - name: Preserve Sprint36 session-control regression
        working-directory: apps/web
        run: php tests/first-party-session-inventory-revocation.php

      - name: Preserve full application regression
        working-directory: apps/web
        run: php tests/run.php

      - name: Preserve activation boundaries
        shell: bash
        run: |
          set -euo pipefail
          test "${ONEQAY_PERSISTENCE_ENABLED}" = "false"
          test "${ONEQAY_AUTHENTICATION_SESSION_CONTROL_ENABLED}" = "false"
          ! grep -R --exclude='sprint37-first-party-all-session-termination-regression.yml' -E 'ONEQAY_TECHNICAL_PREVIEW_ENABLED[[:space:]]*=[[:space:]]*true' apps/web .github/workflows
          grep -F 'Technical Preview remains `NO_SCHEMA_CHANGE`.' docs/FIRST_PARTY_ALL_SESSION_TERMINATION_FOUNDATION.md
          grep -F 'Production remains `NO-GO / NOT AUTHORIZED`.' docs/FIRST_PARTY_ALL_SESSION_TERMINATION_FOUNDATION.md
          grep -F 'Updater remains `DISABLED / UNWIRED`.' docs/FIRST_PARTY_ALL_SESSION_TERMINATION_FOUNDATION.md
