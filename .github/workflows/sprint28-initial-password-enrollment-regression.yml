# Sprint57 immutable sale-to-shift binding compatibility preservation anchor — Lab | zefry
# Sprint54 bounded successor compatibility predecessor — Lab | zefry
# Sprint53 bounded third historical fixture compatibility closure — Lab | zefry
# Sprint53 bounded second historical fixture compatibility closure — Lab | zefry
# Author by Lab | zefry
# Sprint41 Sprint31 source-successor state correction preservation anchor.
name: Sprint 28 Initial Password Enrollment Regression

# Attribution: Lab | zefry

on:
  pull_request:
    branches: [main]
    paths:
      - 'apps/web/app/Application/Identity/InitialPasswordEnrollmentId.php'
      - 'apps/web/app/Application/Identity/IssuedInitialPasswordEnrollment.php'
      - 'apps/web/app/Application/Identity/InitialPasswordEnrollmentRepository.php'
      - 'apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php'
      - 'apps/web/app/Application/Identity/InitialPasswordEnrollmentViolation.php'
      - 'apps/web/app/Infrastructure/Identity/LaravelInitialPasswordEnrollmentRepository.php'
      - 'apps/web/app/Providers/AppServiceProvider.php'
      - 'apps/web/app/Delivery/Http/Identity/InitialPasswordEnrollmentController.php'
      - 'apps/web/routes/web.php'
      - 'apps/web/database/migrations/0000_00_00_000008_create_initial_password_enrollments.php'
      - 'apps/web/tests/initial-password-enrollment.php'
      - '.github/workflows/sprint28-initial-password-enrollment-regression.yml'
      - 'docs/FIRST_PARTY_INITIAL_PASSWORD_ENROLLMENT_FOUNDATION.md'
      - 'apps/web/tests/authorization-persistence.php'
      - 'apps/web/tests/authorization-administration-persistence.php'
      - 'apps/web/tests/initial-tenant-administrator-provisioning.php'
      - 'apps/web/tests/protected-control-administrator-lifecycle.php'
      - 'apps/web/tests/tenant-isolation.php'
      - 'apps/web/tests/identity-org-context.php'
      - 'apps/web/tests/policy-administration-delivery.php'
      - 'apps/web/tests/identity-credential-verification.php'
      - 'apps/web/tests/first-party-session-establishment.php'
      - '.github/workflows/m7-2-tenant-isolation-regression.yml'
      - '.github/workflows/m7-3-identity-org-context-regression.yml'
      - '.github/workflows/m7-4a-technical-preview-interaction-regression.yml'
      - '.github/workflows/m7-5-preview-db-qualification-regression.yml'
      - '.github/workflows/sprint21-role-permission-policy-regression.yml'
      - '.github/workflows/sprint22-policy-administration-regression.yml'
      - '.github/workflows/sprint23-initial-tenant-admin-provisioning-regression.yml'
      - '.github/workflows/sprint24-protected-control-admin-lifecycle-regression.yml'
      - '.github/workflows/sprint25-policy-administration-delivery-regression.yml'
      - '.github/workflows/sprint26-identity-credential-verification-regression.yml'
      - '.github/workflows/sprint27-first-party-session-establishment-regression.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sprint28-initial-password-enrollment-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  sprint28-initial-password-enrollment-regression:
    name: sprint28-initial-password-enrollment-regression
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      APP_NAME: oneQay
      APP_ENV: testing
      APP_KEY: base64:dHR0dHR0dHR0dHR0dHR0dHR0dHR0dHR0dHR0dHR0dHQ=
      APP_DEBUG: 'false'
      APP_URL: http://localhost
      ONEQAY_RUNTIME_CLASS: ci
      ONEQAY_PERSISTENCE_ENABLED: 'false'
      SESSION_DRIVER: array
      CACHE_STORE: array

    steps:
      - name: Checkout exact source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Recognize exact Sprint 30 and Sprint 31 bounded successors
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${{ github.base_ref }}"
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/oneqay-successor-files.txt
          count="$(wc -l < /tmp/oneqay-successor-files.txt)"
          fingerprint="$(sha256sum /tmp/oneqay-successor-files.txt | awk '{print $1}')"
          if [[ "$count" -eq 46 ]] && [[ "$fingerprint" == '95daaf86ba93ae797fccf3825d65d27acd4f71ee58916898a16fbc83d432a5ce' ]]; then
            echo 'ONEQAY_SPRINT30_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT31_SUCCESSOR=false' >> "$GITHUB_ENV"
            echo 'Exact Sprint 30 successor envelope recognized; legacy shape-only guards defer to the dedicated Sprint 30 workflow.'
          elif [[ "$count" -eq 26 ]] && [[ "$fingerprint" == '8136b57a5c9949ec5020a3d5ae497f34431a704900eb0447d42fdb791efb6e39' ]]; then
            echo 'ONEQAY_SPRINT30_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'Exact Sprint 31 successor envelope recognized; stale historical shape guards defer while executable preservation remains active.'
          else
            echo 'ONEQAY_SPRINT30_SUCCESSOR=false' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT31_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Recognize exact Sprint 32 bounded successor
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/s32-successor-files.txt
          count="$(wc -l < /tmp/s32-successor-files.txt)"
          fingerprint="$(sha256sum /tmp/s32-successor-files.txt | awk '{print $1}')"
          if [[ "$count" -eq 32 ]] && [[ "$fingerprint" == 'db230ab3b77fff67f0bd12d7d7b615146d9d9df9a0af12014214e1862e9f6867' ]]; then
            echo 'ONEQAY_SPRINT30_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            rm -f apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
          else
            echo 'ONEQAY_SPRINT32_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Recognize exact Sprint 33 bounded successor
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/s33-successor-files.txt
          count="$(wc -l < /tmp/s33-successor-files.txt)"
          fingerprint="$(sha256sum /tmp/s33-successor-files.txt | awk '{print $1}')"
          if [[ "$count" -eq 35 ]] && [[ "$fingerprint" == 'e3b724002cfc0be1ef890d1b5594a2a5179123f949f6f486354e21950c7328eb' ]]; then
            echo 'ONEQAY_SPRINT30_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT33_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT34_SUCCESSOR=true' >> "$GITHUB_ENV"
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php
            rm -f apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            rm -f apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php
            echo 'Exact Sprint 34 successor envelope recognized; historical executable regression remains active while migration #10 is isolated from legacy migration-shape guards.'
            exit 0
          fi
          if [[ "$count" -eq 39 ]] && [[ "$fingerprint" == '04a1177c12712183a7dda4ae81be1356c0e41294533336c9f999d376c224712a' ]]; then
            echo 'ONEQAY_SPRINT30_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT33_SUCCESSOR=true' >> "$GITHUB_ENV"
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            rm -f apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            echo 'Exact Sprint 33 successor envelope recognized; historical executable regression remains active while migration #10 is isolated from legacy migration-shape guards.'
          else
            echo 'ONEQAY_SPRINT33_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Recognize exact Sprint35 and Sprint36 bounded successors
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "main"
          git diff --name-only "origin/main...HEAD" | sort > /tmp/s35-successor-files.txt
          count="$(wc -l < /tmp/s35-successor-files.txt)"
          fingerprint="$(sha256sum /tmp/s35-successor-files.txt | awk '{print $1}')"
          if { [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]] || [[ "$count" -eq 19 && "$fingerprint" == '563d445adb1c6a2d7caf2fae5a0b694485cddfa5389fc61773ceaa19d978cf79' ]]; }; then
            for migration in \
              'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000019_create_pos_sale_void_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000020_create_pos_inventory_baseline_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000021_create_pos_sale_cash_refund_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000022_create_pos_shift_opening_cash_evidence_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000023_create_pos_shift_closing_cash_evidence_foundation.php'
            do
              if [[ -f "$migration" ]]; then
                git update-index --assume-unchanged "$migration"
                rm -f "$migration"
              fi
            done
            echo 'ONEQAY_S50_SOURCE_COMPATIBILITY=true' >> "$GITHUB_ENV"
            echo 'Sprint50 migration horizon isolated only from exact historical executable preservation.'
          else
            echo 'ONEQAY_S50_SOURCE_COMPATIBILITY=false' >> "$GITHUB_ENV"
          fi
          if [[ "$count" -eq 15 && "$fingerprint" == '5e19664988cabba0030f9927d26b0702370414be4cd6b424d585925b634ca2b8' && -f 'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php'
            echo 'Migration #18 isolated only from exact Sprint48 JRN-005 source historical preservation.'
          fi
          if [[ "$count" -eq 17 && "$fingerprint" == 'e889db1c7eaa22b3ed008f8781ab35652ca950a3f009c309e5c478d01d368f11' ]] || [[ "$count" -eq 18 && "$fingerprint" == '25dbbd94087eba4157fa9c209f09174a127154a98067abbfbeec233bbe9398cd' ]] || [[ "$count" -eq 23 && "$fingerprint" == 'ea735f8f5ee06d480863f9d1ba7ae58a91642109963c3f340e8453f3205bb7ae' ]] || [[ "$count" -eq 19 && "$fingerprint" == 'fc2b578fc9d090dfce1fd34c5acfa7a07f065979a94f62becb3dc54dee664920' ]] || [[ "$count" -eq 14 && "$fingerprint" == 'ed29b6128c193f0efd6359748e220a37aefaec856acc4bc3b90f445ce3ccb674' ]] || [[ "$count" -eq 15 && "$fingerprint" == 'c1ac6d5130c9d78ff99ed0accf5d11e3f08debc575c0d5152ef5b24e6f82ce02' ]] || [[ "$count" -eq 15 && "$fingerprint" == '5e19664988cabba0030f9927d26b0702370414be4cd6b424d585925b634ca2b8' ]] || [[ "$count" -eq 11 && "$fingerprint" == 'a221779e05b1e8ab220610b5068be5d1eb01bc08b516b338ba8f22373e7d89d0' ]] || [[ "$count" -eq 10 && "$fingerprint" == '411950d5602dc7160668c88e08a3941ebccc8bdc82d20bee77ce4004f039d216' ]] || [[ "$count" -eq 8 && "$fingerprint" == 'a9caf2b68210a38687fee543256aec04dc1e67ee1ef403608f7db69139957ff8' ]] || [[ "$count" -eq 12 && "$fingerprint" == 'b2c5fc10a8baa2d56991d6dbd36b0407159d70953654ef322a9a11d23660489b' ]] || [[ "$count" -eq 8 && "$fingerprint" == '6315890d318c3cdfca549bfacef6cb8d1ca66a4421416b49b4978095a98b6729' ]] || [[ "$count" -eq 9 && "$fingerprint" == '3d0293362f451fe4bf472d0d2c38c3eec3d67df75b451ba5b273e8dbdb0f2eed' ]] || [[ "$count" -eq 4 && "$fingerprint" == 'ff01f1355de6c7fdfd28c2d359eb70787dd8448f0b1fc6d9cb73c1a0fb76580a' ]] || [[ "$count" -eq 4 && "$fingerprint" == '5dfaecf9be5c584b431606a7253515ab623ad9a11b4ff74062e794a1f40917c7' ]] || [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]] || [[ "$count" -eq 19 && "$fingerprint" == '563d445adb1c6a2d7caf2fae5a0b694485cddfa5389fc61773ceaa19d978cf79' ]] || [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]] || [[ "$count" -eq 19 && "$fingerprint" == '563d445adb1c6a2d7caf2fae5a0b694485cddfa5389fc61773ceaa19d978cf79' ]]; then
          if [[ -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
            echo 'Migration #15 isolated only from exact Sprint41 source historical preservation.'
          fi
          echo 'ONEQAY_SPRINT35_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT30_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT33_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT34_SUCCESSOR=true' >> "$GITHUB_ENV"
          if [[ -f 'apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000012_add_totp_factor_epoch_and_recovery_authority.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000012_add_totp_factor_epoch_and_recovery_authority.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000012_add_totp_factor_epoch_and_recovery_authority.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000013_create_first_party_session_authority.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000013_create_first_party_session_authority.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000013_create_first_party_session_authority.php'
          fi
          echo 'Exact Sprint35/Sprint36 successor or bounded correction envelope recognized; historical executable preservation remains active.'
          if [[ "$count" -eq 14 && "$fingerprint" == 'ed29b6128c193f0efd6359748e220a37aefaec856acc4bc3b90f445ce3ccb674' ]] || [[ "$count" -eq 15 && "$fingerprint" == 'c1ac6d5130c9d78ff99ed0accf5d11e3f08debc575c0d5152ef5b24e6f82ce02' ]] || [[ "$count" -eq 15 && "$fingerprint" == '5e19664988cabba0030f9927d26b0702370414be4cd6b424d585925b634ca2b8' ]] || [[ "$count" -eq 19 && "$fingerprint" == 'fc2b578fc9d090dfce1fd34c5acfa7a07f065979a94f62becb3dc54dee664920' ]] || [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]] || [[ "$count" -eq 19 && "$fingerprint" == '563d445adb1c6a2d7caf2fae5a0b694485cddfa5389fc61773ceaa19d978cf79' ]]; then
            for migration in \
              'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' \
              'apps/web/database/migrations/0000_00_00_000016_create_pos_sale_completion_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000017_create_pos_catalog_preparation_journal.php'
            do
              if [[ -f "$migration" ]]; then
                git update-index --assume-unchanged "$migration"
                rm -f "$migration"
              fi
            done
            echo 'Migrations #15-#17 isolated only from exact governed POS source historical preservation.'
          fi
          else
            echo 'ONEQAY_SPRINT35_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Recognize exact Sprint70 source and workflow closure for Sprint28
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          base="${{ github.event.pull_request.base.sha }}"
          head="${{ github.event.pull_request.head.sha }}"
          git diff --name-only "$base" "$head" | sort > /tmp/s70-downstream-paths
          count="$(wc -l < /tmp/s70-downstream-paths)"
          fingerprint="$(sha256sum /tmp/s70-downstream-paths | awk '{print $1}')"
          if [[ "$count" -eq 6 && "$fingerprint" == 'cc40f08beda780e4aa4795f29aa7533bbde1029d11855d43f180812502c4d80b' ]] || [[ "$count" -eq 8 && "$fingerprint" == '3e5b516b17bfc4b2b4e8694248672ff7a4298a3e080e0617a70b1bb1d8aa6194' ]] || [[ "$count" -eq 9 && "$fingerprint" == 'c2a575ec728249a8a4b26c173229b26455eee92c7bd4c59026a1d4c064e2c442' ]] || [[ "$count" -eq 4 && "$fingerprint" == 'eaa9c44aed2728b0a508f6be91155f3f2021f7f3de1383164a54be611d3933cf' ]] || [[ "$count" -eq 20 && "$fingerprint" == 'c6cd387d5ea83d73382493ca78d219c0db35477e55c61a68e489a0329231f922' ]]; then
            for index in $(seq 10 25); do
              migration="$(find apps/web/database/migrations -maxdepth 1 -type f -name "$(printf '0000_00_00_%06d_*' "$index")" -print -quit)"
              if [[ -n "$migration" && -f "$migration" ]]; then
                git update-index --assume-unchanged "$migration"
                rm -f "$migration"
              fi
            done
            echo 'ONEQAY_S50_SOURCE_COMPATIBILITY=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT30_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT33_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT34_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT35_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT36_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_WORKFLOW_CORRECTION=true' >> "$GITHUB_ENV"
          fi

      - name: Enforce exact Sprint 28 source envelope
        if: (github.event_name == 'pull_request' && env.ONEQAY_SPRINT30_SUCCESSOR != 'true') && env.ONEQAY_SPRINT35_SUCCESSOR != 'true'
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${{ github.base_ref }}"
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/s29-successor-files.txt
          if [[ "$(wc -l < /tmp/s29-successor-files.txt)" -eq 20 ]] && [[ "$(sha256sum /tmp/s29-successor-files.txt | awk '{print $1}')" == '29ab63415c913c80aed0f19eb97d547cc31c60bd38a2484a0e6dc0f3c8341d03' ]]; then
            echo 'Exact Sprint 29 successor envelope accepted for Sprint 28 preservation.'
            exit 0
          fi
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" > /tmp/sprint28-files.txt
          test -s /tmp/sprint28-files.txt
          while IFS= read -r file; do
            case "$file" in
              apps/web/app/Application/Identity/InitialPasswordEnrollmentId.php|\
              apps/web/app/Application/Identity/IssuedInitialPasswordEnrollment.php|\
              apps/web/app/Application/Identity/InitialPasswordEnrollmentRepository.php|\
              apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php|\
              apps/web/app/Application/Identity/InitialPasswordEnrollmentViolation.php|\
              apps/web/app/Infrastructure/Identity/LaravelInitialPasswordEnrollmentRepository.php|\
              apps/web/app/Providers/AppServiceProvider.php|\
              apps/web/app/Delivery/Http/Identity/InitialPasswordEnrollmentController.php|\
              apps/web/routes/web.php|\
              apps/web/database/migrations/0000_00_00_000008_create_initial_password_enrollments.php|\
              apps/web/tests/initial-password-enrollment.php|\
              .github/workflows/sprint28-initial-password-enrollment-regression.yml|\
              docs/FIRST_PARTY_INITIAL_PASSWORD_ENROLLMENT_FOUNDATION.md|\
              apps/web/tests/authorization-persistence.php|\
              apps/web/tests/authorization-administration-persistence.php|\
              apps/web/tests/initial-tenant-administrator-provisioning.php|\
              apps/web/tests/protected-control-administrator-lifecycle.php|\
              apps/web/tests/tenant-isolation.php|\
              apps/web/tests/identity-org-context.php|\
              apps/web/tests/policy-administration-delivery.php|\
              apps/web/tests/identity-credential-verification.php|\
              apps/web/tests/first-party-session-establishment.php|\
              .github/workflows/m7-2-tenant-isolation-regression.yml|\
              .github/workflows/m7-3-identity-org-context-regression.yml|\
              .github/workflows/m7-4a-technical-preview-interaction-regression.yml|\
              .github/workflows/m7-5-preview-db-qualification-regression.yml|\
              .github/workflows/sprint21-role-permission-policy-regression.yml|\
              .github/workflows/sprint22-policy-administration-regression.yml|\
              .github/workflows/sprint23-initial-tenant-admin-provisioning-regression.yml|\
              .github/workflows/sprint24-protected-control-admin-lifecycle-regression.yml|\
              .github/workflows/sprint25-policy-administration-delivery-regression.yml|\
              .github/workflows/sprint26-identity-credential-verification-regression.yml|\
              .github/workflows/sprint27-first-party-session-establishment-regression.yml)
                ;;
              *) echo "Unauthorized Sprint 28 changed path: $file" >&2; exit 1 ;;
            esac
          done < /tmp/sprint28-files.txt
          test "$(wc -l < /tmp/sprint28-files.txt)" -eq 33
          if grep -Eq '(^|/)(composer\.(json|lock)|package(-lock)?\.json)$' /tmp/sprint28-files.txt; then
            echo 'Sprint 28 authorizes zero dependency changes.' >&2
            exit 1
          fi

      - name: Enforce migration one through seven immutability and migration eight only
        if: github.event_name == 'pull_request' && env.ONEQAY_SPRINT30_SUCCESSOR != 'true'
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/s29-successor-migration-files.txt
          if [[ "$(wc -l < /tmp/s29-successor-migration-files.txt)" -eq 20 ]] && [[ "$(sha256sum /tmp/s29-successor-migration-files.txt | awk '{print $1}')" == '29ab63415c913c80aed0f19eb97d547cc31c60bd38a2484a0e6dc0f3c8341d03' ]]; then
            git diff --exit-code "origin/${{ github.base_ref }}...HEAD" -- apps/web/database/migrations
          else
            base="origin/${{ github.base_ref }}"
            immutable=(
              apps/web/database/migrations/0000_00_00_000001_create_foundational_context_graph.php
              apps/web/database/migrations/0000_00_00_000002_create_organizational_access_grants.php
              apps/web/database/migrations/0000_00_00_000003_create_scoped_role_permission_policy.php
              apps/web/database/migrations/0000_00_00_000004_create_policy_mutation_journal.php
              apps/web/database/migrations/0000_00_00_000005_create_initial_tenant_administrator_provisioning_journal.php
              apps/web/database/migrations/0000_00_00_000006_create_protected_control_administrator_mutation_journal.php
              apps/web/database/migrations/0000_00_00_000007_create_identity_password_credentials.php
            )
            git diff --exit-code "$base...HEAD" -- "${immutable[@]}"
            mapfile -t migration_diff < <(git diff --name-status "$base...HEAD" -- apps/web/database/migrations)
            test "${#migration_diff[@]}" -eq 1
            test "${migration_diff[0]}" = $'A\tapps/web/database/migrations/0000_00_00_000008_create_initial_password_enrollments.php'
          fi

      - name: Preserve root Platform Foundation regression
        run: |
          composer validate --strict --no-check-publish
          composer test

      - name: Install locked application dependencies
        working-directory: apps/web
        run: |
          composer validate --no-check-publish
          composer install --no-interaction --no-progress --prefer-dist

      - name: Lint Sprint 28 enrollment identifier
        run: php -l apps/web/app/Application/Identity/InitialPasswordEnrollmentId.php

      - name: Lint Sprint 28 issued enrollment result
        run: php -l apps/web/app/Application/Identity/IssuedInitialPasswordEnrollment.php

      - name: Lint Sprint 28 repository contract
        run: php -l apps/web/app/Application/Identity/InitialPasswordEnrollmentRepository.php

      - name: Lint Sprint 28 enrollment service
        run: php -l apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php

      - name: Lint Sprint 28 enrollment violation
        run: php -l apps/web/app/Application/Identity/InitialPasswordEnrollmentViolation.php

      - name: Lint Sprint 28 infrastructure repository
        run: php -l apps/web/app/Infrastructure/Identity/LaravelInitialPasswordEnrollmentRepository.php

      - name: Lint Sprint 28 provider composition
        run: php -l apps/web/app/Providers/AppServiceProvider.php

      - name: Lint Sprint 28 HTTP controller
        run: php -l apps/web/app/Delivery/Http/Identity/InitialPasswordEnrollmentController.php

      - name: Lint Sprint 28 routes
        run: php -l apps/web/routes/web.php

      - name: Lint Sprint 28 migration eight
        run: php -l apps/web/database/migrations/0000_00_00_000008_create_initial_password_enrollments.php

      - name: Lint Sprint 28 disposable regression
        run: php -l apps/web/tests/initial-password-enrollment.php

      - name: Lint Sprint 21 preservation test
        run: php -l apps/web/tests/authorization-persistence.php

      - name: Lint Sprint 22 preservation test
        run: php -l apps/web/tests/authorization-administration-persistence.php

      - name: Lint Sprint 23 preservation test
        run: php -l apps/web/tests/initial-tenant-administrator-provisioning.php

      - name: Lint Sprint 24 preservation test
        run: php -l apps/web/tests/protected-control-administrator-lifecycle.php

      - name: Lint M7.2 preservation test
        run: php -l apps/web/tests/tenant-isolation.php

      - name: Lint M7.3 preservation test
        run: php -l apps/web/tests/identity-org-context.php

      - name: Lint Sprint 25 preservation test
        run: php -l apps/web/tests/policy-administration-delivery.php

      - name: Lint Sprint 26 preservation test
        run: php -l apps/web/tests/identity-credential-verification.php

      - name: Lint Sprint 27 preservation test
        run: php -l apps/web/tests/first-party-session-establishment.php

      - name: Validate PHP syntax and exact eight-migration set
        if: env.ONEQAY_SPRINT30_SUCCESSOR != 'true'
        shell: bash
        run: |
          set -euo pipefail
          find apps/web/app apps/web/bootstrap apps/web/config apps/web/database apps/web/public apps/web/routes apps/web/tests -type f -name '*.php' -print0 |
            while IFS= read -r -d '' file; do php -l "$file"; done
          mapfile -t migrations < <(find apps/web/database/migrations -maxdepth 1 -type f -name '*.php' -printf '%f\n' | sort)
          expected=(
            '0000_00_00_000001_create_foundational_context_graph.php'
            '0000_00_00_000002_create_organizational_access_grants.php'
            '0000_00_00_000003_create_scoped_role_permission_policy.php'
            '0000_00_00_000004_create_policy_mutation_journal.php'
            '0000_00_00_000005_create_initial_tenant_administrator_provisioning_journal.php'
            '0000_00_00_000006_create_protected_control_administrator_mutation_journal.php'
            '0000_00_00_000007_create_identity_password_credentials.php'
            '0000_00_00_000008_create_initial_password_enrollments.php'
          )
          test "${migrations[*]}" = "${expected[*]}"

      - name: Enforce Application enrollment boundary
        shell: bash
        run: |
          set -euo pipefail
          files=(
            apps/web/app/Application/Identity/InitialPasswordEnrollmentId.php
            apps/web/app/Application/Identity/IssuedInitialPasswordEnrollment.php
            apps/web/app/Application/Identity/InitialPasswordEnrollmentRepository.php
            apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php
            apps/web/app/Application/Identity/InitialPasswordEnrollmentViolation.php
          )
          if grep -n -E 'Illuminate\\|Schema::|DB::|new PDO|mysqli_|ConnectionInterface|->table\(' "${files[@]}"; then
            echo 'Sprint 28 Application enrollment source gained framework/database mechanics.' >&2
            exit 1
          fi
          grep -Fq 'TOKEN_TTL_SECONDS = 900' apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php
          grep -Fq 'MIN_PASSWORD_BYTES = 12' apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php
          grep -Fq 'MAX_PASSWORD_BYTES = 4096' apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php
          grep -Fq '#[\SensitiveParameter]' apps/web/app/Application/Identity/InitialPasswordEnrollmentRepository.php
          grep -Fq '#[\SensitiveParameter]' apps/web/app/Application/Identity/InitialPasswordEnrollmentService.php

      - name: Enforce migration eight secret-minimal schema
        shell: bash
        run: |
          set -euo pipefail
          migration='apps/web/database/migrations/0000_00_00_000008_create_initial_password_enrollments.php'
          grep -Fq "Schema::create('oneqay_initial_password_enrollments'" "$migration"
          grep -Fq "string('tenant_id', 64)" "$migration"
          grep -Fq "string('enrollment_id', 64)" "$migration"
          grep -Fq "string('actor_identity_id', 96)" "$migration"
          grep -Fq "string('target_identity_id', 96)" "$migration"
          grep -Fq "char('token_digest', 64)" "$migration"
          grep -Fq "unique(" "$migration"
          grep -Fq "uq_initial_password_enrollment_active_target" "$migration"
          grep -Fq 'fk_initial_password_enrollment_actor' "$migration"
          grep -Fq 'fk_initial_password_enrollment_target' "$migration"
          grep -Fq 'Forward-only generated migration; rollback is not authorized.' "$migration"
          if grep -Ei 'plaintext|password_hash|password[^_a-z]|session|csrf|email|phone|metadata' "$migration"; then
            echo 'Migration eight contains forbidden secret/profile fields.' >&2
            exit 1
          fi

      - name: Enforce secure token and insert-only credential repository
        shell: bash
        run: |
          set -euo pipefail
          repo='apps/web/app/Infrastructure/Identity/LaravelInitialPasswordEnrollmentRepository.php'
          grep -Fq 'random_bytes(32)' "$repo"
          grep -Fq "hash('sha256', \$token)" "$repo"
          grep -Fq "hash('sha256', \$enrollmentToken)" "$repo"
          grep -Fq 'hash_equals(' "$repo"
          grep -Fq 'password_hash($password, PASSWORD_DEFAULT)' "$repo"
          grep -Fq "['local', 'test', 'ci']" "$repo"
          grep -Fq 'AdministrationPermission::MANAGE' "$repo"
          grep -Fq "where('a.tenant_id', \$tenantId)" "$repo"
          grep -Fq "where('a.identity_id', \$identityId)" "$repo"
          test "$(grep -Fc 'table(self::CREDENTIAL_TABLE)->insert([' "$repo")" -eq 1
          if grep -Eq 'table\(self::CREDENTIAL_TABLE\).*(update|updateOrInsert|upsert|delete|truncate)' "$repo"; then
            echo 'Sprint 28 credential path gained overwrite/delete mechanics.' >&2
            exit 1
          fi
          if grep -Ei '\b(log|logger|debug|info|warning|error)\s*\(' "$repo"; then
            echo 'Sprint 28 repository gained logging mechanics.' >&2
            exit 1
          fi

      - name: Enforce bounded HTTP routes and delivery privacy
        if: env.ONEQAY_SPRINT30_SUCCESSOR != 'true'
        shell: bash
        run: |
          set -euo pipefail
          routes='apps/web/routes/web.php'
          controller='apps/web/app/Delivery/Http/Identity/InitialPasswordEnrollmentController.php'
          grep -Fq "Route::post('/administration/identity/password-enrollments'" "$routes"
          grep -Fq "Route::post('/auth/password-enrollment'" "$routes"
          grep -Fq "name('identity.initial-password-enrollment.issue')" "$routes"
          grep -Fq "name('auth.initial-password-enrollment.redeem')" "$routes"
          grep -Fq 'RequirePolicyAdministrationSessionContextMiddleware::class' "$routes"
          grep -Fq "['local', 'test', 'ci']" "$routes"
          grep -Fq 'INITIAL_PASSWORD_ENROLLMENT_ISSUE_REJECTED' "$controller"
          grep -Fq 'INITIAL_PASSWORD_ENROLLMENT_FAILED' "$controller"
          grep -Fq "'Cache-Control' => 'no-store, private'" "$controller"
          grep -Fq "'Pragma' => 'no-cache'" "$controller"
          if grep -n -E 'password_(verify|hash)\s*\(|random_bytes\(|Schema::|DB::|new PDO|mysqli_|->table\(' "$controller"; then
            echo 'Sprint 28 controller gained cryptographic/database mechanics.' >&2
            exit 1
          fi
          if grep -Fq 'FirstPartySessionKeys::' "$controller" || grep -Fq -- '->session()->put' "$controller"; then
            echo 'Sprint 28 enrollment delivery gained automatic login/session establishment.' >&2
            exit 1
          fi
          if grep -Ei '\b(log|logger|debug|info|warning|error)\s*\(' "$controller"; then
            echo 'Sprint 28 controller gained logging mechanics.' >&2
            exit 1
          fi

      - name: Prove runtime-scoped Sprint 28 route registration
        working-directory: apps/web
        shell: bash
        run: |
          set -euo pipefail
          ci_routes="$(ONEQAY_RUNTIME_CLASS=ci php artisan route:list --path=password-enrollment 2>&1)"
          grep -Fq 'administration/identity/password-enrollments' <<<"$ci_routes"
          grep -Fq 'auth/password-enrollment' <<<"$ci_routes"
          preview_routes="$(ONEQAY_RUNTIME_CLASS=preview php artisan route:list --path=password-enrollment 2>&1)"
          if grep -Fq 'password-enrollment' <<<"$preview_routes"; then
            echo 'Sprint 28 enrollment routes are active in Preview.' >&2
            exit 1
          fi
          production_routes="$(ONEQAY_RUNTIME_CLASS=production php artisan route:list --path=password-enrollment 2>&1)"
          if grep -Fq 'password-enrollment' <<<"$production_routes"; then
            echo 'Sprint 28 enrollment routes are active in Production.' >&2
            exit 1
          fi

      - name: Preserve Technical Preview Production updater and persistence defaults
        shell: bash
        run: |
          set -euo pipefail
          grep -Fq 'ONEQAY_PERSISTENCE_ENABLED=false' apps/web/environment.example
          grep -Fq '/apps/web/database/migrations/' .github/workflows/m7-5-preview-release-artifact.yml
          grep -Fq 'NO_SCHEMA_CHANGE' .github/workflows/m7-5-preview-release-artifact.yml
          grep -Fq 'php tests/privileged-update-security.php' .github/workflows/sprint22-policy-administration-regression.yml
          if grep -R -n -E 'InitialPasswordEnrollment|password-enrollment' apps/web/app/Application/Preview apps/web/app/Delivery/Preview 2>/dev/null; then
            echo 'Technical Preview gained Sprint 28 enrollment wiring.' >&2
            exit 1
          fi

      - name: Run Sprint 28 disposable enrollment regression
        working-directory: apps/web
        run: php tests/initial-password-enrollment.php

      - name: Preserve Sprint 21 through Sprint 27 regressions
        working-directory: apps/web
        shell: bash
        run: |
          set -euo pipefail
          composer test
          php -r 'require "vendor/autoload.php"; $app=require "bootstrap/app.php"; $app->make(Illuminate\Contracts\Http\Kernel::class)->bootstrap(); $assert=static function(bool $c,string $m):void{if(!$c)throw new RuntimeException($m);}; require "tests/authorization-persistence.php"; require "tests/authorization-administration-persistence.php"; require "tests/initial-tenant-administrator-provisioning.php"; require "tests/protected-control-administrator-lifecycle.php";'
          php tests/tenant-isolation.php
          php tests/identity-org-context.php
          php tests/policy-administration-delivery.php
          php tests/identity-credential-verification.php
          php tests/first-party-session-establishment.php
          php tests/privileged-update-security.php

      - name: Preserve tracked source
        run: git diff --exit-code