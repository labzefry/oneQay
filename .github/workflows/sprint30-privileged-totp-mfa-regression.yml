# Author by Lab | zefry
# Sprint41 Sprint31 source-successor state correction preservation anchor.
name: Sprint 30 Privileged TOTP MFA Regression

# Attribution: Lab | zefry

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/m7-2-tenant-isolation-regression.yml'
      - '.github/workflows/m7-3-identity-org-context-regression.yml'
      - '.github/workflows/m7-4a-technical-preview-interaction-regression.yml'
      - '.github/workflows/m7-5-preview-db-qualification-regression.yml'
      - '.github/workflows/sprint21-role-permission-policy-regression.yml'
      - '.github/workflows/sprint22-policy-administration-regression.yml'
      - '.github/workflows/sprint23-initial-tenant-admin-provisioning-regression.yml'
      - '.github/workflows/sprint24-protected-control-admin-lifecycle-regression.yml'
      - '.github/workflows/sprint25-policy-administration-delivery-regression.yml'
      - '.github/workflows/sprint26-identity-credential-verification-regression.yml'
      - '.github/workflows/sprint27-first-party-session-establishment-regression.yml'
      - '.github/workflows/sprint28-initial-password-enrollment-regression.yml'
      - '.github/workflows/sprint29-first-control-principal-credential-bootstrap-regression.yml'
      - '.github/workflows/sprint30-privileged-totp-mfa-regression.yml'
      - 'apps/web/app/Application/Identity/IssuedPrivilegedTotpEnrollment.php'
      - 'apps/web/app/Application/Identity/PrivilegedTotpClock.php'
      - 'apps/web/app/Application/Identity/PrivilegedTotpEngine.php'
      - 'apps/web/app/Application/Identity/PrivilegedTotpMfaRepository.php'
      - 'apps/web/app/Application/Identity/PrivilegedTotpMfaService.php'
      - 'apps/web/app/Application/Identity/PrivilegedTotpMfaState.php'
      - 'apps/web/app/Application/Identity/PrivilegedTotpMfaViolation.php'
      - 'apps/web/app/Delivery/Http/Identity/FirstPartySessionController.php'
      - 'apps/web/app/Delivery/Http/Identity/FirstPartySessionKeys.php'
      - 'apps/web/app/Delivery/Http/Identity/PrivilegedTotpMfaController.php'
      - 'apps/web/app/Delivery/Http/Middleware/RequirePolicyAdministrationSessionContextMiddleware.php'
      - 'apps/web/app/Infrastructure/Identity/LaravelPrivilegedTotpMfaRepository.php'
      - 'apps/web/app/Infrastructure/Identity/OtphpPrivilegedTotpEngine.php'
      - 'apps/web/app/Providers/AppServiceProvider.php'
      - 'apps/web/composer.json'
      - 'apps/web/composer.lock'
      - 'apps/web/config/oneqay.php'
      - 'apps/web/database/migrations/0000_00_00_000009_create_identity_totp_factors.php'
      - 'apps/web/routes/web.php'
      - 'apps/web/tests/authorization-administration-persistence.php'
      - 'apps/web/tests/authorization-persistence.php'
      - 'apps/web/tests/first-control-principal-credential-bootstrap.php'
      - 'apps/web/tests/first-party-session-establishment.php'
      - 'apps/web/tests/identity-credential-verification.php'
      - 'apps/web/tests/identity-org-context.php'
      - 'apps/web/tests/initial-password-enrollment.php'
      - 'apps/web/tests/initial-tenant-administrator-provisioning.php'
      - 'apps/web/tests/policy-administration-delivery.php'
      - 'apps/web/tests/privileged-totp-mfa.php'
      - 'apps/web/tests/protected-control-administrator-lifecycle.php'
      - 'apps/web/tests/tenant-isolation.php'
      - 'docs/PRIVILEGED_TOTP_MFA_FOUNDATION.md'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sprint30-privileged-totp-mfa-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  sprint30-privileged-totp-mfa-regression:
    name: sprint30-privileged-totp-mfa-regression
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      APP_NAME: oneQay
      APP_ENV: testing
      APP_KEY: base64:bW1tbW1tbW1tbW1tbW1tbW1tbW1tbW1tbW1tbW1tbW0=
      APP_DEBUG: 'false'
      APP_URL: http://localhost
      ONEQAY_RUNTIME_CLASS: ci
      ONEQAY_PERSISTENCE_ENABLED: 'false'
      ONEQAY_PRIVILEGED_TOTP_MFA_ENABLED: 'false'
      SESSION_DRIVER: array
      CACHE_STORE: array

    steps:
      - name: Checkout exact source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Setup exact PHP runtime
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240
        with:
          php-version: '8.3'
          tools: composer:v2
          coverage: none

      - name: Recognize exact Sprint 31 bounded successor
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${{ github.base_ref }}"
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/s31-successor-files.txt
          if [[ "$(wc -l < /tmp/s31-successor-files.txt)" -eq 26 ]] && [[ "$(sha256sum /tmp/s31-successor-files.txt | awk '{print $1}')" == '8136b57a5c9949ec5020a3d5ae497f34431a704900eb0447d42fdb791efb6e39' ]]; then
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
          else
            echo 'ONEQAY_SPRINT31_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Recognize exact Sprint 32 bounded successor
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/s32-successor-files.txt
          count="$(wc -l < /tmp/s32-successor-files.txt)"
          fingerprint="$(sha256sum /tmp/s32-successor-files.txt | awk '{print $1}')"
          if [[ "$count" -eq 32 ]] && [[ "$fingerprint" == 'db230ab3b77fff67f0bd12d7d7b615146d9d9df9a0af12014214e1862e9f6867' ]]; then
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            rm -f apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
          else
            echo 'ONEQAY_SPRINT32_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Recognize exact Sprint 33 bounded successor
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/s33-successor-files.txt
          count="$(wc -l < /tmp/s33-successor-files.txt)"
          fingerprint="$(sha256sum /tmp/s33-successor-files.txt | awk '{print $1}')"
          if [[ "$count" -eq 35 ]] && [[ "$fingerprint" == 'e3b724002cfc0be1ef890d1b5594a2a5179123f949f6f486354e21950c7328eb' ]]; then
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT33_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT34_SUCCESSOR=true' >> "$GITHUB_ENV"
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php
            rm -f apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            rm -f apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php
            echo 'Exact Sprint 34 successor envelope recognized; Sprint 30 executable preservation remains active.'
            exit 0
          fi
          if [[ "$count" -eq 39 ]] && [[ "$fingerprint" == '04a1177c12712183a7dda4ae81be1356c0e41294533336c9f999d376c224712a' ]]; then
            echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
            echo 'ONEQAY_SPRINT33_SUCCESSOR=true' >> "$GITHUB_ENV"
            git update-index --assume-unchanged apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            rm -f apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php
            echo 'Exact Sprint 33 successor envelope recognized; Sprint 30 executable preservation remains active.'
          else
            echo 'ONEQAY_SPRINT33_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Recognize exact Sprint35 and Sprint36 bounded successors
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "main"
          git diff --name-only "origin/main...HEAD" | sort > /tmp/s35-successor-files.txt
          count="$(wc -l < /tmp/s35-successor-files.txt)"
          fingerprint="$(sha256sum /tmp/s35-successor-files.txt | awk '{print $1}')"
          if { [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]]; }; then
            for migration in \
              'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000019_create_pos_sale_void_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000020_create_pos_inventory_baseline_foundation.php'
            do
              if [[ -f "$migration" ]]; then
                git update-index --assume-unchanged "$migration"
                rm -f "$migration"
              fi
            done
            echo 'ONEQAY_S50_SOURCE_COMPATIBILITY=true' >> "$GITHUB_ENV"
            echo 'Sprint50 migration horizon isolated only from exact historical executable preservation.'
          else
            echo 'ONEQAY_S50_SOURCE_COMPATIBILITY=false' >> "$GITHUB_ENV"
          fi
          if [[ "$count" -eq 15 && "$fingerprint" == '5e19664988cabba0030f9927d26b0702370414be4cd6b424d585925b634ca2b8' && -f 'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000018_create_pos_shift_opening_foundation.php'
            echo 'Migration #18 isolated only from exact Sprint48 JRN-005 source historical preservation.'
          fi
          if [[ "$count" -eq 17 && "$fingerprint" == 'e889db1c7eaa22b3ed008f8781ab35652ca950a3f009c309e5c478d01d368f11' ]] || [[ "$count" -eq 18 && "$fingerprint" == '25dbbd94087eba4157fa9c209f09174a127154a98067abbfbeec233bbe9398cd' ]] || [[ "$count" -eq 23 && "$fingerprint" == 'ea735f8f5ee06d480863f9d1ba7ae58a91642109963c3f340e8453f3205bb7ae' ]] || [[ "$count" -eq 19 && "$fingerprint" == 'fc2b578fc9d090dfce1fd34c5acfa7a07f065979a94f62becb3dc54dee664920' ]] || [[ "$count" -eq 14 && "$fingerprint" == 'ed29b6128c193f0efd6359748e220a37aefaec856acc4bc3b90f445ce3ccb674' ]] || [[ "$count" -eq 15 && "$fingerprint" == 'c1ac6d5130c9d78ff99ed0accf5d11e3f08debc575c0d5152ef5b24e6f82ce02' ]] || [[ "$count" -eq 15 && "$fingerprint" == '5e19664988cabba0030f9927d26b0702370414be4cd6b424d585925b634ca2b8' ]] || [[ "$count" -eq 11 && "$fingerprint" == 'a221779e05b1e8ab220610b5068be5d1eb01bc08b516b338ba8f22373e7d89d0' ]] || [[ "$count" -eq 10 && "$fingerprint" == '411950d5602dc7160668c88e08a3941ebccc8bdc82d20bee77ce4004f039d216' ]] || [[ "$count" -eq 8 && "$fingerprint" == 'a9caf2b68210a38687fee543256aec04dc1e67ee1ef403608f7db69139957ff8' ]] || [[ "$count" -eq 12 && "$fingerprint" == 'b2c5fc10a8baa2d56991d6dbd36b0407159d70953654ef322a9a11d23660489b' ]] || [[ "$count" -eq 8 && "$fingerprint" == '6315890d318c3cdfca549bfacef6cb8d1ca66a4421416b49b4978095a98b6729' ]] || [[ "$count" -eq 9 && "$fingerprint" == '3d0293362f451fe4bf472d0d2c38c3eec3d67df75b451ba5b273e8dbdb0f2eed' ]] || [[ "$count" -eq 4 && "$fingerprint" == 'ff01f1355de6c7fdfd28c2d359eb70787dd8448f0b1fc6d9cb73c1a0fb76580a' ]] || [[ "$count" -eq 4 && "$fingerprint" == '5dfaecf9be5c584b431606a7253515ab623ad9a11b4ff74062e794a1f40917c7' ]] || [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]] || [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]]; then
          if [[ -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000014_add_first_party_authentication_eligibility_to_identities.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php'
            echo 'Migration #15 isolated only from exact Sprint41 source historical preservation.'
          fi
          echo 'ONEQAY_SPRINT35_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT31_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT32_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT33_SUCCESSOR=true' >> "$GITHUB_ENV"
          echo 'ONEQAY_SPRINT34_SUCCESSOR=true' >> "$GITHUB_ENV"
          if [[ -f 'apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000010_create_identity_recovery_codes.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000011_add_credential_epoch_to_identity_password_credentials.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000012_add_totp_factor_epoch_and_recovery_authority.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000012_add_totp_factor_epoch_and_recovery_authority.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000012_add_totp_factor_epoch_and_recovery_authority.php'
          fi
          if [[ -f 'apps/web/database/migrations/0000_00_00_000013_create_first_party_session_authority.php' ]]; then
            git update-index --assume-unchanged 'apps/web/database/migrations/0000_00_00_000013_create_first_party_session_authority.php'
            rm -f 'apps/web/database/migrations/0000_00_00_000013_create_first_party_session_authority.php'
          fi
          echo 'Exact Sprint35/Sprint36 successor or bounded correction envelope recognized; historical executable preservation remains active.'
          if [[ "$count" -eq 14 && "$fingerprint" == 'ed29b6128c193f0efd6359748e220a37aefaec856acc4bc3b90f445ce3ccb674' ]] || [[ "$count" -eq 15 && "$fingerprint" == 'c1ac6d5130c9d78ff99ed0accf5d11e3f08debc575c0d5152ef5b24e6f82ce02' ]] || [[ "$count" -eq 15 && "$fingerprint" == '5e19664988cabba0030f9927d26b0702370414be4cd6b424d585925b634ca2b8' ]] || [[ "$count" -eq 19 && "$fingerprint" == 'fc2b578fc9d090dfce1fd34c5acfa7a07f065979a94f62becb3dc54dee664920' ]] || [[ "$count" -eq 14 && "$fingerprint" == '78628976e4dbfea29ecab4b40ec6bfbdf697564668ef028a5cb77a3b53466451' ]] || [[ "$count" -eq 15 && "$fingerprint" == '325559599c79ba68340eac34569f8e5513aa4957b1ddf2515007b98b2cc1f15f' ]] || [[ "$count" -eq 30 && "$fingerprint" == 'cf8adb2328427a8e27e46f0efd9fe88db90f0401eba9cafd513944c7e040035e' ]] || [[ "$count" -eq 28 && "$fingerprint" == '3b97670674b60c636a23e52551b7ade69a3f545d5d0b4a861fa1c06eae5c0656' ]]; then
            for migration in \
              'apps/web/database/migrations/0000_00_00_000015_create_identity_authentication_eligibility_administration_journal.php' \
              'apps/web/database/migrations/0000_00_00_000016_create_pos_sale_completion_foundation.php' \
              'apps/web/database/migrations/0000_00_00_000017_create_pos_catalog_preparation_journal.php'
            do
              if [[ -f "$migration" ]]; then
                git update-index --assume-unchanged "$migration"
                rm -f "$migration"
              fi
            done
            echo 'Migrations #15-#17 isolated only from exact governed POS source historical preservation.'
          fi
          else
            echo 'ONEQAY_SPRINT35_SUCCESSOR=false' >> "$GITHUB_ENV"
          fi

      - name: Enforce exact Sprint 30 source envelope
        if: (github.event_name == 'pull_request' && env.ONEQAY_SPRINT31_SUCCESSOR != 'true') && env.ONEQAY_SPRINT35_SUCCESSOR != 'true'
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${{ github.base_ref }}"
          git diff --name-only "origin/${{ github.base_ref }}...HEAD" | sort > /tmp/sprint30-files.txt
          test "$(wc -l < /tmp/sprint30-files.txt)" -eq 46
          actual="$(sha256sum /tmp/sprint30-files.txt | awk '{print $1}')"
          expected='95daaf86ba93ae797fccf3825d65d27acd4f71ee58916898a16fbc83d432a5ce'
          test "$actual" = "$expected"
          if grep -Eq '(^|/)\.env($|\.)' /tmp/sprint30-files.txt; then exit 1; fi
          if grep -Eq '(^|/)package(-lock)?\.json$' /tmp/sprint30-files.txt; then exit 1; fi
          mapfile -t composer_diff < <(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- apps/web/composer.json apps/web/composer.lock | sort)
          test "${composer_diff[*]}" = 'apps/web/composer.json apps/web/composer.lock'

      - name: Enforce migration nine additive-only contract
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == 'pull_request' ]] && [[ "${ONEQAY_SPRINT31_SUCCESSOR:-false}" != 'true' ]]; then
            base="origin/${{ github.base_ref }}"
            for migration in \
              0000_00_00_000001_create_foundational_context_graph.php \
              0000_00_00_000002_create_organizational_access_grants.php \
              0000_00_00_000003_create_scoped_role_permission_policy.php \
              0000_00_00_000004_create_policy_mutation_journal.php \
              0000_00_00_000005_create_initial_tenant_administrator_provisioning_journal.php \
              0000_00_00_000006_create_protected_control_administrator_mutation_journal.php \
              0000_00_00_000007_create_identity_password_credentials.php \
              0000_00_00_000008_create_initial_password_enrollments.php
            do
              git diff --exit-code "$base...HEAD" -- "apps/web/database/migrations/$migration"
            done
            mapfile -t migration_diff < <(git diff --name-status "$base...HEAD" -- apps/web/database/migrations)
            test "${#migration_diff[@]}" -eq 1
            test "${migration_diff[0]}" = $'A\tapps/web/database/migrations/0000_00_00_000009_create_identity_totp_factors.php'
          fi
          mapfile -t migrations < <(find apps/web/database/migrations -maxdepth 1 -type f -name '*.php' -printf '%f\n' | sort)
          expected=(
            '0000_00_00_000001_create_foundational_context_graph.php'
            '0000_00_00_000002_create_organizational_access_grants.php'
            '0000_00_00_000003_create_scoped_role_permission_policy.php'
            '0000_00_00_000004_create_policy_mutation_journal.php'
            '0000_00_00_000005_create_initial_tenant_administrator_provisioning_journal.php'
            '0000_00_00_000006_create_protected_control_administrator_mutation_journal.php'
            '0000_00_00_000007_create_identity_password_credentials.php'
            '0000_00_00_000008_create_initial_password_enrollments.php'
            '0000_00_00_000009_create_identity_totp_factors.php'
          )
          test "${migrations[*]}" = "${expected[*]}"
          ! find apps/web/database/migrations -maxdepth 1 -type f -name '*000010*' -print -quit | grep -q .
          migration='apps/web/database/migrations/0000_00_00_000009_create_identity_totp_factors.php'
          grep -Fq "Schema::create('oneqay_identity_totp_factors'" "$migration"
          grep -Fq "string('tenant_id', 64)" "$migration"
          grep -Fq "string('identity_id', 96)" "$migration"
          grep -Fq "text('secret_ciphertext')" "$migration"
          grep -Fq "unsignedBigInteger('created_at_unix')" "$migration"
          grep -Fq "unsignedBigInteger('confirmed_at_unix')->nullable()" "$migration"
          grep -Fq "unsignedBigInteger('last_accepted_time_step')->nullable()" "$migration"
          grep -Fq "primary(['tenant_id', 'identity_id']" "$migration"
          grep -Fq 'fk_identity_totp_factor_identity' "$migration"
          grep -Fq "throw new LogicException('Forward-only generated migration; rollback is not authorized.')" "$migration"

      - name: Preserve root Platform Foundation regression
        run: |
          composer validate --strict --no-check-publish
          composer test

      - name: Validate exact application Composer dependency
        working-directory: apps/web
        shell: bash
        run: |
          set -euo pipefail
          composer validate --no-check-publish
          php -r '$j=json_decode(file_get_contents("composer.json"),true,512,JSON_THROW_ON_ERROR); if (($j["require"]["spomky-labs/otphp"] ?? null) !== "11.5.0") exit(1);'
          php -r '$j=json_decode(file_get_contents("composer.lock"),true,512,JSON_THROW_ON_ERROR); $p=[]; foreach($j["packages"] as $x){$p[$x["name"]]=$x;} if (($p["spomky-labs/otphp"]["version"] ?? null)!=="11.5.0") exit(1); if (($p["spomky-labs/otphp"]["source"]["reference"] ?? null)!=="877683d6352b80cdc7020fd43a725629c2524435") exit(1); if (($p["paragonie/constant_time_encoding"]["version"] ?? null)!=="v3.1.3") exit(1); if (($p["paragonie/constant_time_encoding"]["source"]["reference"] ?? null)!=="d5b01a39b3415c2cd581d3bd3a3575c1ebbd8e77") exit(1);'
          composer install --no-interaction --no-progress --prefer-dist
          composer show --locked spomky-labs/otphp | grep -F '11.5.0'
          composer show --locked paragonie/constant_time_encoding | grep -F '3.1.3'

      - name: Reject High or Critical Composer advisories
        working-directory: apps/web
        shell: bash
        run: |
          set -euo pipefail
          composer audit --locked --format=json > /tmp/oneqay-s30-composer-audit.json || true
          php -r '$data=json_decode(file_get_contents("/tmp/oneqay-s30-composer-audit.json"),true,512,JSON_THROW_ON_ERROR); foreach (($data["advisories"] ?? []) as $package=>$items) { foreach ($items as $item) { if (in_array(strtolower((string)($item["severity"] ?? "")), ["high","critical"], true)) { fwrite(STDERR,"Blocking Composer advisory for {$package}.\n"); exit(1); } } }'

      - name: Lint Sprint 30 PHP source and preserved fixtures
        shell: bash
        run: |
          set -euo pipefail
          find apps/web/app apps/web/database/migrations apps/web/routes apps/web/tests -type f -name '*.php' -print0 | while IFS= read -r -d '' file; do php -l "$file"; done

      - name: Enforce framework-independent Application and provider-only OTPHP boundary
        shell: bash
        run: |
          set -euo pipefail
          application=(
            apps/web/app/Application/Identity/IssuedPrivilegedTotpEnrollment.php
            apps/web/app/Application/Identity/PrivilegedTotpClock.php
            apps/web/app/Application/Identity/PrivilegedTotpEngine.php
            apps/web/app/Application/Identity/PrivilegedTotpMfaRepository.php
            apps/web/app/Application/Identity/PrivilegedTotpMfaService.php
            apps/web/app/Application/Identity/PrivilegedTotpMfaState.php
            apps/web/app/Application/Identity/PrivilegedTotpMfaViolation.php
          )
          if grep -n -E 'Illuminate\\|OTPHP\\|Schema::|DB::|new PDO|mysqli_|ConnectionInterface|Request|Session' "${application[@]}"; then exit 1; fi
          mapfile -t otphp_imports < <(grep -RIl -E '^use OTPHP\\|OTPHP\\' apps/web/app || true)
          test "${#otphp_imports[@]}" -eq 1
          test "${otphp_imports[0]}" = 'apps/web/app/Infrastructure/Identity/OtphpPrivilegedTotpEngine.php'
          if grep -RInE 'hash_hmac|base32|dynamic truncation|GoogleChart|chart.googleapis|quickchart|qrcode' "${application[@]}" apps/web/app/Delivery/Http/Identity/PrivilegedTotpMfaController.php; then exit 1; fi

      - name: Enforce fixed TOTP profile encrypted binding and monotonic replay control
        shell: bash
        run: |
          set -euo pipefail
          engine='apps/web/app/Infrastructure/Identity/OtphpPrivilegedTotpEngine.php'
          repo='apps/web/app/Infrastructure/Identity/LaravelPrivilegedTotpMfaRepository.php'
          grep -Fq "PERIOD_SECONDS = 30" "$engine"
          grep -Fq "DIGITS = 6" "$engine"
          grep -Fq "DIGEST = 'sha1'" "$engine"
          grep -Fq "SECRET_BYTES = 20" "$engine"
          grep -Fq "ISSUER = 'oneQay'" "$engine"
          grep -Fq 'verify($code, $timestamp, null)' "$engine"
          grep -Fq 'encryptString($payload)' "$repo"
          grep -Fq 'decryptString($ciphertext)' "$repo"
          grep -Fq "'tenant_id' => \$tenantId" "$repo"
          grep -Fq "'identity_id' => \$identityId" "$repo"
          grep -Fq 'lockForUpdate()' "$repo"
          grep -Fq "where('last_accepted_time_step', '<', \$matchedTimeStep)" "$repo"
          grep -Fq "update(['last_accepted_time_step' => \$matchedTimeStep])" "$repo"
          if grep -Eq 'table\(self::TABLE\).*(delete|truncate|upsert|updateOrInsert)' "$repo"; then exit 1; fi

      - name: Enforce fail-closed feature arm exact session vocabulary and bounded routes
        shell: bash
        run: |
          set -euo pipefail
          config='apps/web/config/oneqay.php'
          keys='apps/web/app/Delivery/Http/Identity/FirstPartySessionKeys.php'
          routes='apps/web/routes/web.php'
          middleware='apps/web/app/Delivery/Http/Middleware/RequirePolicyAdministrationSessionContextMiddleware.php'
          grep -Fq "env('ONEQAY_PRIVILEGED_TOTP_MFA_ENABLED', false)" "$config"
          for literal in \
            'oneqay.auth.pending.identity_id' \
            'oneqay.auth.pending.tenant_id' \
            'oneqay.auth.pending.organization_id' \
            'oneqay.auth.pending.outlet_id' \
            'oneqay.auth.pending.device_id' \
            'oneqay.auth.pending.mfa_state' \
            'oneqay.auth.mfa_verified_at' \
            'enrollment_required' \
            'challenge_required'
          do grep -Fq "$literal" "$keys"; done
          test "$(grep -Fc "Route::post('/auth/mfa/totp/" "$routes")" -eq 3
          grep -Fq "'/auth/mfa/totp/enrollment/start'" "$routes"
          grep -Fq "'/auth/mfa/totp/enrollment/confirm'" "$routes"
          grep -Fq "'/auth/mfa/totp/challenge'" "$routes"
          grep -Fq "['local', 'test', 'ci']" "$routes"
          grep -Fq 'MFA_VERIFIED_AT' "$middleware"
          grep -Fq 'mfa_verified_at' "$keys"

      - name: Enforce no secret leakage and Preview Production updater separation
        shell: bash
        run: |
          set -euo pipefail
          if grep -RInE 'PrivilegedTotp|privileged_totp|mfa/totp' apps/web/app/Application/Preview apps/web/app/Delivery/Preview 2>/dev/null; then exit 1; fi
          grep -Fq "'install_enabled' => false" apps/web/config/oneqay.php
          grep -Fq 'NO_SCHEMA_CHANGE' docs/PRIVILEGED_TOTP_MFA_FOUNDATION.md
          grep -Fq 'NO-GO / NOT AUTHORIZED' docs/PRIVILEGED_TOTP_MFA_FOUNDATION.md
          grep -Fq 'DISABLED / UNWIRED' docs/PRIVILEGED_TOTP_MFA_FOUNDATION.md
          if grep -RInE 'echo .*secret|print.*secret|var_dump\(.*secret|logger.*secret|Log::.*secret' apps/web/app apps/web/tests/privileged-totp-mfa.php; then exit 1; fi

      - name: Run disposable Sprint 30 privileged TOTP MFA regression
        run: php apps/web/tests/privileged-totp-mfa.php

      - name: Preserve Sprint 21 through Sprint 24 with application harness
        working-directory: apps/web
        shell: bash
        run: |
          set -euo pipefail
          php -r 'require "vendor/autoload.php"; $app=require "bootstrap/app.php"; $app->make(Illuminate\Contracts\Http\Kernel::class)->bootstrap(); $assert=static function(bool $c,string $m):void{if(!$c)throw new RuntimeException($m);}; require "tests/authorization-persistence.php"; require "tests/authorization-administration-persistence.php"; require "tests/initial-tenant-administrator-provisioning.php"; require "tests/protected-control-administrator-lifecycle.php";'

      - name: Preserve M7 tenant identity and Sprint 25 through Sprint 29 regressions
        working-directory: apps/web
        shell: bash
        run: |
          set -euo pipefail
          php tests/tenant-isolation.php
          php tests/identity-org-context.php
          php tests/policy-administration-delivery.php
          php tests/identity-credential-verification.php
          php tests/first-party-session-establishment.php
          php tests/initial-password-enrollment.php
          php tests/first-control-principal-credential-bootstrap.php
          php tests/privileged-update-security.php

      - name: Confirm tracked source cleanliness
        run: test -z "$(git status --porcelain --untracked-files=no)"