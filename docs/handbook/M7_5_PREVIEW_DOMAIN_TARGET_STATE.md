# M7.5 Preview Domain Target State

Attribution: **Lab | zefry**

## Current Product Owner direction

Technical Preview / tester hostname:

`oneqay.n07.my.id`

Future Production public hostname intent:

`oneqay.com`

## Authority boundary

This document records target intent only.

It does not authorize:

- domain creation;
- DNS mutation;
- Cloudflare mutation;
- certificate installation;
- application deployment;
- M7.6;
- Release;
- Production.

At the 2026-08-15 evidence intake, the supplied cPanel Domains and SSL/TLS evidence does not show `oneqay.n07.my.id` as an already-provisioned target. Therefore Preview hostname provisioning, document-root mapping, certificate coverage, HTTPS redirect behavior, and effective oneQay request routing remain external runtime evidence items.

The intended future `oneqay.com` hostname must remain completely separated from Technical Preview authority until Production is separately approved.
