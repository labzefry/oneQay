# Diagram Sources

Store reviewable diagram source here. Prefer Mermaid or another text-based format that can be diffed. Diagram labels must avoid secrets, real tenant data, credentials, or production topology details that create unnecessary exposure.
